BASIC++ CHANGELOG
=================

5.0.5 (2026-07-10) -- Universal Color Scheme Integration & Directory Cleanup
---------------------------------------------------------------------------------
  Editor & Terminal Enhancements:
  - Implemented 'F10' hotkey across all native internal text editors (--edlin, --ws, --edit, --vi) to dynamically toggle syntax color highlighting on/off.
  - Ensured --edlin natively boots into monochrome mode by default (color_mode = 0), requiring manual color toggle via the 'o' command.
  - Hardened terminal state restoration by explicitly passing the ANSI reset sequence (\x1b[0m\x1b[2J\x1b[H) on exit in mod_edlin.c and mod_ws.c, preventing color bleeding into the parent shell.
  - Safely replaced overlapping strcpy/strcat memory modifications with standard memmove across mod_vi.c and mod_edit.c, eliminating GCC -Wrestrict warnings.
  - Ported missing _strdup implementation in mod_ws.c to plat_strdup for cross-platform POSIX standard compatibility on GCC/Linux.

  Repository Maintenance & Code Health:
  - Purged all legacy Python patch scripts, test batch runners, leftover debug logs (*.LOG, *.OUT), text dumps, and temporary .BAS scratchpads from the root directory.
  - Deleted intermediate .obj/.o build artifacts, .map files, and deprecated *old.exe executables to ensure a pristine and reproducible build environment.
  - Successfully verified 100% clean compilation on both Windows MSVC and Linux GCC across all variations (basicpp, basicpp-console, blite, bppc, trans, 1964, level1, apple2, tinybasic).

5.0.4 (2026-07-10) -- Universal OS Clipboard & CUA Editor Selection
---------------------------------------------------------------------------------
  Editor Subsystems:
  - Universal OS Clipboard Hooks: Implemented robust dual-layer clipboard access covering Windows API (OpenClipboard, GetClipboardData) and SDL2 (SDL_SetClipboardText). Fallbacks gracefully to internal process buffers if OS clipboards are unavailable.
  - Added full visual CUA (Shift+Arrows) multi-line highlighting in --edit (MS-DOS EDIT), --vi, and --ws (WordStar).
  - Implemented legacy hotkeys: Ctrl+Ins (Copy), Shift+Del (Cut), and Shift+Ins (Paste) across all native internal text editors without colliding with native modes or command structures.
  - Eliminated screen tearing and flicker via buffered line painting.
  - Adjusted MS-DOS EDIT clone menu to include [RUN] mapped functionally to execution.

5.0.3 (2026-07-10) -- Documentation Accuracy Refresh & C17 Modernization
---------------------------------------------------------------------------------
  Documentation:
  - Dynamically generated completely accurate 'CATALOG.txt' and 'HELP.txt' files from the core engine representation. 
  - Verified and documented all 371 live keywords natively through automated help queries.
  - Eliminated all undocumented stubs and updated 'Quick_Reference.txt' and 'README.md' to reflect accurate feature sets.

  Refactoring & Architecture:
  - Decomposed the monolithic 'help.c' database into strictly categorized modular source files (help_math.c, help_strings.c, etc.).
  - Applied strict modern ISO/IEC 9899:2018 (C17) guidelines across the engine, refactoring legacy C89 style loops and syntax.
  - Resolved dynamic scope mapping and corrected symbol binding errors under strict GCC and MSVC MSBuild toolchains.
5.0.2 (2026-07-09) -- Automatic GW-BASIC Detokenization
---------------------------------------------------------------------------------
  Major Features:
  - Implemented automatic binary detokenization for GW-BASIC files in LOAD command.
  - Transparently runs external in_detok.exe utility on binary .BAS files and loads the resulting plain text script into the interpreter memory.
  - Successfully maintained interpreter constraints by strictly calling external binary format handlers.
 5.0.1 (2026-07-09) -- Game Engine & Library Refactoring ---------------------------------------------------------------------------------   Major Features:   - Completely refactored all 11 built-in games (Adventure, Zelda, Pacman, Mario, etc.) from hardcoded logic scripts into modular Game Engines.   - Extracted core generic game mechanics into independent library files in the `lib/` directory: swarm.LIB, maze.LIB, platformer.LIB, arcade_shooter.LIB, rpg_procgen.LIB, boardgame.LIB, and vfx.LIB.   - Implemented procedural generation algorithms for Adventure and Zelda to ensure new dungeons and maps are built randomly upon every execution.   - Introduced `.SAV` file mechanics across all games, allowing players to press `S` to Save and `L` to Load their progress.   - Ensured strict separation between logic and text-based TUI character rendering (no SDL required).   - Updated SOURCE_TREE.txt to reflect structural changes.  5.0.0 (2026-07-07) -- Built-in Line Editor & RNG/GUID$ Enhancements ---------------------------------------------------------------------------------   Major Features:   - Integrated the Edlin line editor into BASIC++ Standard (Console and SDL).     Launched via the new '--edlin [file]' command-line switch.   - Edlin is a fully decoupled module (mod_edlin.c / mod_edlin.h) that     communicates with the core interpreter exclusively through a callback     contract (EdlinCallbacks), enabling easy porting to other projects.   - The 'x' command in the editor executes the buffer contents as a BASIC++     program and returns the user to the editor prompt afterward.   - Supports both numbered and unnumbered lines. If lines lack line numbers,     the execution callback auto-numbers them (10, 20, 30...) transparently.   - Editor commands: list, insert, delete, edit, copy, move, page, search,     replace, transfer, write, append, execute, save+exit, quit.   - This marks the beginning phase of implementing optional line numbers     in BASIC++ programs.    RNG & GUID$ Enhancements:   - Exhaustively validated all RND variants: RND, RND(0), RND(1), RND(n),     RND(-n), RND &Hnnn, RND &Onnn, RND &Bnnn, RND TIME.   - Implemented GUID$ string function: generates RFC 4122 v4 UUIDs using     the deterministic PRNG engine. Seeded sequences produce repeatable UUIDs.   - Registered KW_GUID_FUNC across lexer, funcreg, and help system.   - Created exhaustive documentation for RND and GUID$ in docs/ and help/.    Compilation & Code Health:   - All builds (Console, SDL, Lite, trans, bppc) compile with zero warnings     under MSVC C17 /W3.   - SELFTEST passes all 13 subsystem checks.   - Updated catalog.TXT, help.TXT, quick_reference.MD, and quick_reference.TXT.  4.9.12 (2026-07-07) -- I/O Keyword Validation & Expansion ---------------------------------------------------------------------------------   Compilation & Code Health:   - Validated and verified I/O keyword categories (Block I/O, Stream I/O, Network I/O, Persistent I/O, Transaction I/O).   - Implemented missing network I/O functions: NWRITE and NREAD$.   - Developed progressive regression testing suites in tests/io/[Subcategory].   - Cleaned up I/O keyword documentation into subdirectories within docs/io and help/io.   - Re-sorted catalog.TXT, help.TXT, and quick_reference files alphabetically and applied new subcategories.   - Achieved 100% test pass rate across all Linux and Windows builds (Console and SDL).  4.9.11 (2026-07-07) -- Time & Date Variables and Version Synchronization ---------------------------------------------------------------------------------   Compilation & Code Health:   - Fixed lexer mapping for `TODAY$` causing it to parse as an uninitialized variable.   - Implemented evaluation logic in `parser_expr.c` for `TODAY$` using `gmtime()` for ISO 8601 UTC string output.   - Validated and verified date/time variables: `TI`, `TI$`, `TIME`, `TIME$`, `DATE`, `DATE$`, `DAY`, `DAY$`, `DOW`, `TICKS`, `JIFFIES`, `TODAY$`.   - Added modular Markdown and plaintext documentation for all date/time variables.   - Updated `config.h` version macro constants to correctly reflect version 4.9.11 in compiled binaries.   - Conducted full source code vs changelog feature integration audit to verify keyword implementations.  4.9.10 (2026-07-06) -- Devices & Networking Keyword Validation ---------------------------------------------------------------------------------   Compilation & Code Health:   - Validated and verified 100% success rate for Devices & Hardware keywords (INP, OUT, WAIT, SYS, BIOREAD, SIOREAD, SIOREADLN, COM, IOCTL).   - Validated and verified 100% success rate for Networking & Virtualization keywords (VDEV, VNET, VMEM, VMACH, VCON, VTERM, DEVMAP, NINFO, NJSONQUERY, GOPHER, GEMINI).   - Built dynamic TLS loading system in proxy fallback mode for GEMINI on Windows and Linux.   - Added modular documentation files for Devices & Hardware and Networking & Virtualization categories under `docs/devices_networking` and `help/devices_networking`.   - Split and documented logical/bitwise operators (`AND`, `OR`, `NOT`, `XOR`, `EQV`, `IMP`) into a dedicated `logic_and_bitwise` category, including extreme depth for NLP indexing.   - Relocated and documented the arithmetic `MOD` operator into the `math` category.   - Verified and strictly tested JOSS-style postfix conditionals (`IF`, `UNLESS`, `FOR`) to seamlessly integrate with logical boolean expressions (e.g. `PRINT 777 IF 1 AND 3`).  4.9.9  (2026-07-06) -- Debugging & Program Management Keyword Validation ---------------------------------------------------------------------------------   Compilation & Code Health:   - Cleaned up leftover `printf("DEBUG_HOW: ...")` leakages throughout the entire codebase (`source/io/`, `source/core/`, etc.) by properly using `error_raise(ERR_HOW, line_num);`.   - Audited `printf` calls and replaced them with `gw_printf` internally mapped via `#include "console.h"`, strictly removing raw standard library prints to preserve SDL GUI output parity.   - Successfully achieved 100% regression pass rate for all 15 Debugging keywords (`ASSERT`, `BACKTRACE`, `BREAK`, `CHECK`, `CONT`, `DEBUG`, `DUMP`, `ENDTEST`, `SELFTEST`, `STOP`, `TRACE`, `TROFF`, `TRON`, `VARS`, `VERIFY`).   - Successfully achieved 100% regression pass rate for all 16 Program Management keywords (`BLOAD`, `BRUN`, `BSAVE`, `CHAIN`, `CLOAD`, `COMPILE`, `CSAVE`, `LIST`, `LLIST`, `LOAD`, `MERGE`, `NEW`, `RUN`, `SAVE`, `SCRATCH`, `UNSAVE`).   - Added modular documentation files for Debugging and Program Management categories into `docs/` and `help/`.   - Added progressive test suites `tests/debugging/test_debug.bas` and `tests/progmgmt/test_progmgmt.bas`.   4.9.8  (2026-07-05) -- Control Flow Keyword Audit & Bug Hunts ---------------------------------------------------------------------------------   Compilation & Code Health:   - Fixed `DEFINE SUB` parameter parsing mismatch (resolving `ERR_WHAT` during execution).   - Fixed `SHARED` variable restoration inside `scope_stack_pop` where previously undeclared shared variables were deleted from the caller scope.   - Resolved `lib_space_try_call_func` conflicting definition warnings on Linux.   - Eliminated multiple definition warnings of `pi_parse_shared_cmd` during the Linux lite build via `lite_stubs.c` cleanup.   - Added comprehensive Control Flow regression suite (`tests/test_flow.bas`) fully testing branching, loops, error trapping, procedures, and data iterations.   - Documented `.BAS` and `.C` file comment requirements in `AGENTS.md` (Progressive Testing Mandate).   - All Control Flow keywords verified 100% bug-free and completely compliant with QBASIC and ECMA-116 standards.  4.9.7  (2026-07-04) -- Exhaustive Warning & Error Elimination ---------------------------------------------------------------------------------   Compilation & Code Health:   - Resolved all GCC and MSVC compiler warnings (including string truncation, format overflow, unused variables, implicit declarations, implicit returns, uninitialized variables) across the entire codebase.   - Ensured 100% clean compilation on both Windows (MSVC) and Linux (GCC) for BASIC++ Standard, BASIC++ Lite, BASIC++ SDL, 'trans', and 'compile'.   - Added safe string handling limits using precision specifiers and bounds checking in `codegen.c`, `platform.c`, `builtins_fileio.c`, `ext_feature.c`, `ext_plugin.c`, `mod_usb.c`, and `mod_fujinet.c`.   - Added unused variable pragmas and casts (`(void)var`, `MAYBE_UNUSED`) to safely suppress platform-specific variables without functional regressions.   - Implemented 'SHARED' and 'PUBLIC' keyword tracking in variable system.   - Fixed variable clearing logic in 'CHAIN' to preserve only 'COMMON' variables.   - Hardened runtime execution to correctly trap file I/O errors ('ERR_HOW') in 'RUN', 'LOAD', 'MERGE', and 'CHAIN'.   - Added support for 'SAVE' to include flags for Protected and Obfuscated output.   - Successfully verified execution of internal interpreter SELFTEST diagnostics for parser precedence, flow control, variable assignment, and string pooling on both console and SDL builds.  4.9.6  (2026-07-04) -- Implicit Shell Fallback and REFORMAT keyword ---------------------------------------------------------------------------------   Functional Upgrades:   - Added 'ImplicitShellFallback' functionality allowing the interpreter to act as a system shell in direct mode. Unrecognized commands are passed to the OS shell transparently.   - Added optional hidden profile execution on startup using 'Profile=' configuration key (e.g., PROFILE.BAS) to initialize variables silently before reaching the REPL.   - Implemented the 'REFORMAT' command in direct mode to perform auto-indentation and structural formatting of code currently stored in the memory buffer. Indentation defaults to 4 spaces, configurable via 'REFORMAT [n]'.   4.9.5  (2026-07-04) -- Help Database Core Keyword Restorations and Introspection Verification ---------------------------------------------------------------------------------   Introspection & Help Database:   - Resolved a bug where core statements/commands (PRINT, INPUT, LET, IF, DATA, ELSE) were completely missing from the standard/SDL help database (help_db).   - Restored core statements/commands to standard help_db in correct alphabetical sequence so that 'HELP PRINT', 'HELP INPUT', 'HELP LET', 'HELP IF', 'HELP DATA', 'HELP ELSE' resolve correctly on both console and GUI configurations.   - Audited decoupled subsystems (VFS, Segmented Memory, Virtual Devices, Network, Console, Machine) to verify stability and compatibility after modular separation from interpreter core.  4.9.4  (2026-07-04) -- SDL Emulation Boot Screen Initialization and SCREEN Crash Fix ---------------------------------------------------------------------------------   SDL Boot & Screen fixes:   - Resolved blank/transparent screen rendering bugs in GUI builds ('basicpp.exe' and 'baspp') by explicitly calling `gw_sdl2_clear_screen` inside `gw_sdl2_init` to initialize screen character grid to spaces (' ') and pixel buffer to black.   - Fixed a null pointer dereference (`NULL->banner_text`) inside `handle_screen` in `interp.c` when executing the `SCREEN` command or changing display modes in SDL.  4.9.3  (2026-07-04) -- Transpiler Variable Scope, Standalone Native Compilation, and Python EOF Support ---------------------------------------------------------------------------------   Standalone Native Compilation:   - Added target `standalone` compilation to `bppc` supporting zero-toolchain executable distribution via precompiled runner stubs.   - Implemented cross-compilation target platform selection (`--platform win|linux|dos`) to bundle stubs with appended `.BPE` payloads.   - Resolved PE executable corruption by making CRC header rewrite-back offsets relative to the stub runner size.   - Exposed `--runner` template path selection switch in the interpreter to customize embedded stubs.   - Added BPE signature detection and offset calculation using `bpe_find_embedded_offset` to bypass standard boot flows and run payloads directly.    Transpiler Naming & Scope Isolation:   - Resolved global and local variable collisions by mapping named variables dynamically (e.g. `n1`, `n2` instead of overlapping `n`) in Python 3 and C17 backends.   - Implemented dynamic index allocation (`get_numvar_index` / `get_strvar_index`) in C17 code generation to support arbitrary named variables cleanly without buffer/array overlap.   - Added named loop variable support for `STMT_FOR` and `STMT_NEXT` matching in both C17 and Python 3.   - Fixed infinite loop transpilation bugs on closed stdin by integrating `bpp_input_str` and `bpp_input_num` helper functions with EOF detection.    Control Flow & Parser:   - Updated `build_if` parsing in `ast.c` to recursively chain all sibling colon-separated statements inside the `then_stmt` block, preventing fall-through logic bugs.  4.9.2  (2026-07-04) -- Cross-Platform Compilation & Heading Rebuild Fixes ---------------------------------------------------------------------------------   Linux Build & Header Portability:   - Added headless compilation configuration (`trans-linux` target in Makefile) using `LITE_SOURCES` and `-DBPP_LITE_BUILD` to build the command-line transpiler `trans` without graphical or SDL2 system library dependencies.   - Resolved implicit declaration warnings/errors by defining POSIX/GNU feature test macros (`_POSIX_C_SOURCE`, `_DEFAULT_SOURCE`, `_GNU_SOURCE`) at the top of `platform.c` and `bppc.c` under GCC/Linux.   - Eliminated GCC buffer truncation warnings in `bppc.c` by upgrading `compile_cmd` to 2048 bytes.   - Fixed Windows/Linux dual compilation stub linker symbols by restricting the compile of stubs in `lite_stubs.c` strictly to `BPP_LITE_BUILD` targets.  4.9.1  (2026-07-04) -- Transpilation Comments & Compiler Portability Fixes ---------------------------------------------------------------------------------   Transpiler Upgrades:   - Injected developer reference header comments into all generated C17 and Python 3 source files explaining subsystems, memory management, portability, and troubleshooting guidelines.   - Refined AST transpiler generators to conform strictly to Python 3 and ANSI C17 standards.    Compiler Orchestrator (bppc) Portability:   - Added native platform path normalization (`normalize_to_native`) for Windows and POSIX executions to prevent command invocation errors.   - Implemented forward-slash path normalization (`normalize_to_forward`) for Python bytecode inline compilation arguments to bypass backslash escape exceptions.   - Added math library link flags (`-lm`) to the GCC compilation command on Linux/POSIX.   - Configured version-specific dynamic linking (`-lpython3.x`) for Python stub binary launcher under Linux/POSIX.  4.9.0  (2026-07-04) -- External Compiler and Transpiler Orchestration ---------------------------------------------------------------------------------   Functional Upgrades & Transpiler:   - Created standalone transpiler trans.exe/trans to translate BASIC++ code (.BAS, .BPE, .BPP, .BPL) to strict C17 (default) or Python 3.   - Resolved print statement colon separator parsing bug in AST code generation.   - Fixed INPUT statement semicolon/comma prompt separator bug in AST builder.   - Implemented compiler orchestrator bppc.exe/bppc to run trans and compile generated code to native executables via MSVC, Watcom, or gcc.   - Engineered safe, warning-free direct Python C API marshalling launcher stubs using PyMarshal_ReadObjectFromString and PyEval_EvalCode, completely bypassing compiler string literal limits.   - Successfully transpiled and compiled 191 retro/classic BASIC programs in classics library to C17 and Python 3 native binaries.  4.8.0  (2026-07-04) -- Legacy Dialects Feature Parity & Robustness ---------------------------------------------------------------------------------   Functional Upgrades & Dialect Parity:   - Created 1964.c as a standalone interpreter in strict C89, fully mimicking the historic Dartmouth 1964 dialect (double math, historic precedence, 1D/2D DIM, FN).   - [BUGFIX] Prevented editing keywords (AUTO, DELETE, EDIT, REFORMAT, RENUM, RENAME) from executing inside a running script to avoid memory corruption; they now throw a WHAT? (Illegal direct) error.   - [UPDATE] Expanded documentation for memory management (MEM, SIZE, FRE), virtual memory (POKEB, PEEKB, BANK), multitasking (TASK), editing (AUTO, DELETE, EDIT, REFORMAT, RENUM, RENAME), configuration (ALIAS, OPTION, SCOPE, KEYWORD, OVERRIDE), and diagnostics (CHECK, VERIFY).   - [FEATURE] Changed RENAME syntax to RENAME <type> <old_name> AS <new_name> (where <type> can be SUB, FUNCTION, LABEL, VAR, ARRAY, MATRIX) and added AST manipulation to trace/update references.   - Added built-in lightweight HELP command to all three interpreters.   - Implemented portable, Win32-compatible BEEP statements in all three dialects.   - Implemented auto-uppercase processing for all input lines (preserving quoted strings).   - Enforced mandatory LET keyword across all three interpreters (bare A=5 is now a SYNTAX ERROR).   - Added support for # operator as an alias for <> (not-equal).   - Integrated LOAD/SAVE filename resolving with case-insensitive auto-.BAS extension and optional quote stripping.   - Implemented RUN filename argument checks (raising SYNTAX ERROR).   - Added ABS(), RND(), and SIZE functions to tinybasic.c.   - Added ABS(), INT(), RND(), MEM, TAB(), DATA/READ/RESTORE, ON...GOTO, and CONT to level1.c.   - Added ABS(), SGN(), RND(), MOD, ^, AND/OR/NOT, ON...GOTO, DEL, CLR, CON, AUTO/MAN, string variables (slicing, ASC, LEN), and multi-character variables to apple2.c.   - Added comprehensive section comments to all three interpreters.    Compilation & Validation:   - Ensured 100% warning-free and error-free compilation under MSVC (/TC /W4 /WX /Za) and GCC (-ansi -Wall -Wextra -Werror -pedantic).   - Developed and executed comprehensive comparative validation suite verifying all dialect behaviors on both Windows and Linux.  4.7.0  (2026-07-04) -- Legacy Dialect Refactoring & Rename ---------------------------------------------------------------------------------   Renamed Files:   - core-bas.c / core-bas.exe renamed to level1.c / level1.exe   - int-bas.c / int-bas.exe renamed to apple2.c / apple2.exe    level1.c (TRS-80 Level I BASIC):   - Upgraded arithmetic from 8-bit signed char to 16-bit signed short.   - Made LET keyword mandatory (bare A=5 produces SYNTAX ERROR).   - Removed GOSUB/RETURN, LPRINT, SYSTEM, QUIT/EXIT.   - Added BYE command (clean exit, replaces QUIT/EXIT).   - Kept left-to-right expression evaluation (no operator precedence).   - PRINT supports semicolons and comma tab stops.   - Full IF/THEN with all relational operators (= <> < > <= >=).    apple2.c (Apple II Integer BASIC):   - Upgraded arithmetic from 8-bit signed char to 16-bit signed short.   - Upgraded expression parser to standard operator precedence (* / before + -).   - Made LET keyword mandatory.   - Added DIM statement for 1D integer arrays (DIM A(100)).   - Added TAB() function for PRINT column positioning.   - Added BYE command (clean exit).   - Removed GOSUB/RETURN, LPRINT, BEEP, SYSTEM, QUIT/EXIT, STOP.   - Removed $IMPORT/$INCLUDE/$MERGE stubs and --debug mode.   - Full IF/THEN with all relational operators.   - LET supports array element assignment (LET A(5) = 10).    Updated build.bat, .gitignore, and AGENTS.md to reference new filenames.  4.6.0  (2026-07-04) -- LIST Command Search & Segment Extensions ---------------------------------------------------------------------------------   Search Capability in LIST:   - Extended the `LIST` command with robust multi-segment searches and wildcard matching.   - Quoted pattern search (e.g. `LIST "A$"`) performs a case-insensitive substring search anywhere in the line text (matching comments and string literals).   - Unquoted variable search (e.g. `LIST A$`, `LIST A*`, `LIST A?`) performs exact identifier/variable matching within executable code (ignoring comments and string literals) with case-insensitive wildcard support.   - Label and function search (e.g. `LIST :name`, `LIST :*label*`) searches for both definitions (e.g., `name:`, `SUB name`, `FUNCTION name`, `DEF FNname`) and references/calls (e.g. `GOTO name`, `CALL name`, `FNname`) matching the pattern.   - Resolved colon conflict by implementing a temporary lookahead lexer so that `:` is only parsed as a search prefix when followed by an identifier, allowing statement separation (e.g. `LIST : PRINT ...`) to remain intact.   - Implemented `program_list_search` and contextual matchers in `source/core/memory.c` using standard C17 compliant, memory-optimized algorithms.    Segment Ranges:   - Enabled listing of multiple disjoint segment ranges in a single comma-separated command, e.g. `LIST 10-50, 100-` or `LIST 10, :my_label`.    Documentation:   - Documented LIST command search syntaxes, wildcard behaviors, and examples in `README.md`, `docs/README.md`, `docs/Quick_Reference.md`, `help/Quick_Reference.txt`, and built-in help (`HELP.txt`).    BASIC++ Lite size optimizations:   - Enabled Link-Time Optimization (`/GL`, `/LTCG`) and size minimization (`/O1`, `/Ob0`, `/OPT:REF`, `/OPT:ICF`) on Windows/MSVC, reducing the binary size by approximately 10%.   - Added compile and linker-level size optimizations (`-Os`, `-flto`, `-ffunction-sections`, `-fdata-sections`, `-Wl,--gc-sections`, and `strip`) for Linux/GCC target.   - Implemented missing stubs for external module unloading, AST execution, and program compilation inside `lite_stubs.c` to satisfy the strict global code optimizer during Lite compilation.  4.5.0  (2026-07-04) -- Hexadecimal, Octal, and Binary Line Numbers & Literals ---------------------------------------------------------------------------------   Line Number Bases Support:   - Implemented hexadecimal, octal, and binary line numbers in the REPL (interactive mode) and file loaders (`fileio.c` and `archive.c`).   - Added support for hex prefixes (`&H`/`&h`, `0x`/`0X`), octal prefixes (`&O`/`&o`, `0o`/`0O`, and bare `&`), and binary prefixes (`&B`/`&b`, `0b`/`0B`).   - Allowed hexadecimal, octal, and binary line numbers to range up to `4294967295` (32-bit unsigned limit), while decimal line numbers remain capped at `65529` (`LINE_NUMBER_MAX`).   - Preserved the original input base and prefix representation of line numbers on program listings (`LIST`).   - Supported resolution of jump targets (e.g. `GOTO`, `GOSUB`, `RESTORE`, and `DELETE`) using base prefixes in expressions.   - Implemented a unified helper function `program_parse_line_number` in `source/core/memory.c` to parse multiple base line numbers from text strings cleanly.    Lexer Literals:   - Added support for C-style literal prefixes (`0x`/`0X`, `0o`/`0O`, `0b`/`0B`) in expressions within the tokenizer (`lexer.c`), ensuring consistency with line number target expressions.    Documentation:   - Documented new line number representations and bases in `README.md`, `docs/Quick_Reference.md`, `help/Quick_Reference.txt`, and built-in help (`HELP.txt`).  4.4.7  (2026-07-04) -- VAX/VMS BASIC Compatibility & String Built-ins ---------------------------------------------------------------------------------   VAX/VMS BASIC Compatibility:   - Implemented VAX-style line continuation via trailing ampersand (`&`) in the file loader (`fileio_load`), merging continuation lines with their succeeding lines.   - Implemented `EDIT$(S$, C)` built-in function to perform string manipulation/formatting (compressing, trimming, discarding spaces, case conversions, bracket mapping, etc.) using bitwise mask codes.   - Implemented `NUM$(N)` built-in function to format numeric values as a string (with a leading space for positive numbers, a leading minus for negative numbers, and no trailing space).   - Whitelisted `KW_EDIT` and `KW_NUM` in the lexer (`lexer.c`) to ensure `EDIT$` and `NUM$` are tokenized as keyword functions instead of variables.   - Fixed a critical porting bug in `runtime_collect_data` where `TOK_FLOAT_LIT` was mistakenly processed as an integer, resolving Windows vs Linux platform execution mismatch on reading float values from `DATA` statements.      Conversational Dialects (JOSS, FOCAL, MUMPS) Compatibility:   - Implemented JOSS/FOCAL fractional/decimal line numbers (e.g. `10.10`) translation at load/merge time. Converts step `10.10` to standard line `1010` and injects `STEP_10_10:` label.   - Implemented FOCAL-style `DO` block/step execution: `DO 10.10` executes step `10.10` (mapped to label `STEP_10_10` or line `1010`) as an implicit subroutine.   - Implemented MUMPS-style persistent variables: `PSTORE(key$, val)` writes a persistent value to `vfs/persist/<key>.dat`, and `PRETRIEVE(key$)` / `PRETRIEVE$(key$)` retrieves it.   - Implemented virtual device `PERSIST:<key>` for reading/writing persistent data directly from/to persistent files.   - Whitelisted `KW_PSTORE` and `KW_PRETRIEVE` in `lexer.c` to handle `PRETRIEVE$` properly and avoid naming collisions.    Documentation & Comment Cleanup:   - Cleaned up obsolete references to deleted dialect files and `DIALECT_` enum/constants inside `config.h` and `SOURCE_TREE.txt` to align with the unified single-dialect architecture.    ECMA-116 Standard BASIC Compatibility:   - Conducted full regression validation of ECMA-116 (Full BASIC) features including block exception handling (`WHEN EXCEPTION IN / USE / END WHEN`), matrix math operators (`MAT IDN`, `MAT TRN`, `MAT INV`), and singular matrix exceptions.   - Verified local `WHEN EXCEPTION` handler precedence over global `ON ERROR GOTO` traps.  4.4.6  (2026-07-04) -- HP TSB Hybrid Substring Slicing, String Capacities & Built-ins ---------------------------------------------------------------------------------   Consolidation & Uniform Dialect:   - Removed obsolete custom dialect files (`custom_dialects.c`, `custom_dialects.h`) and unified all behaviors under a single uniform dialect engine.      HP TSB Hybrid Substring Slicing & Capacities:   - Implemented bracket-based substring slicing `A$[start, end]` (or `A$[start]`) as inclusive 1-based substring slices, while `A$(idx)` retains array semantics.   - Implemented capacity-backed scalar string variables via bracketed DIM: `DIM A$[capacity]` declares a string variable with a maximum capacity. Assigning strings exceeding this capacity automatically truncates the string.   - Properly distinguished bracketed DIM array elements: `DIM A$[l, h]` declares a 1D string array where each element has a maximum capacity of `h` characters (truncated upon assignment), whereas `DIM A$(l, h)` declares a standard 2D string array.   - Bracketed 2D DIM array `DIM A$[l, h, c]` declares a 2D string array where each element is capacity-limited to `c` characters.      New Built-in Functions:   - Registered and implemented runtime behaviors for `LGT` (base-10 logarithm), `TIM` (time components for minutes, hours, days, years), `HI` (high 8 bits of a 16-bit integer), and `LO` (low 8 bits of a 16-bit integer).  4.4.5  (2026-07-03) -- DEC BASIC Plus Compatibility & RHS Keyword Rewrite ---------------------------------------------------------------------------------   DEC BASIC Plus Compatibility:   - Implemented virtual arrays (`DIM #channel, array_spec = record_size`) backed by direct seek, binary read, and binary write execution logic.   - Implemented postfix `WHILE` and `UNTIL` loop modifiers for statement suffix execution.   - Fixed variable splitting on identifiers with trailing digits (like `IM3`) in the lexer by only performing trailing digit back-off when the prefix is a keyword.   - Resolved context-aware RHS keyword variable parsing: fallback to treating math/builtin keywords as variables if not followed by parentheses or requiring arguments.  4.4.4  (2026-07-03) -- Systematic Renaming & Codebase Cleanup Refactoring ---------------------------------------------------------------------------------   Systematic File/Folder Renaming:   - Renamed all GWBASIC/QBasic-prefixed files and folders to logical, descriptive names (e.g., `mbf_math`, `segmented_mem`, `compat_plugin`, `sdl2_emu`, `serial_compat`, `legacy_compat`, and `structured_*` test files) to conform with domain structure rules.   - Fixed `#include` directives across all header and source files to point to the new logical filenames.   - Refactored `Makefile` and Linux build script to reflect the new source configuration.      Codebase & Temporary Artifacts Cleanup:   - Deleted all leftover debug `.LOG` files, temporary `.WAV`/`.TAP` audio tape outputs, and root-level BASIC scratch files.   - Cleaned up python helper utilities and one-off scratch scripts inside `scratch/` (except the main test suite runner).   - Deleted old test `.log` outputs inside the `tests/` directory to ensure clean environment isolation.    Tymshare Super BASIC Compatibility & Precedence Safety:   - Implemented context-aware keyword parsing for keywords (such as `UNLESS`, `COMPLEX`, `REAL`, `IMAG`, `CONJ`, `CABS`) to fallback to named variables in expression factor evaluation or statement starts when followed by `=` (assignment context).   - Tightened postfix scanning algorithm in `postfix_scan` to check preceding tokens (preventing matching after operators or delimiters) and peek at succeeding tokens (preventing matching empty modifiers or binary operator start symbols).   4.4.3  (2026-07-02) -- Regression Audit, GRAPHICS Crash Fix & Platform Abstraction ---------------------------------------------------------------------------------   Regression Audit & Critical Bug Fixes:   - Fixed a crash when executing the Atari 8-bit `GRAPHICS` command by adding the missing `case KW_GRAPHICS:` dispatch entry in `parser.c`. The keyword was registered in the lexer but never wired to the parser dispatch table.   - Removed a spurious `lexer_next(lex)` call in `pi_parse_graphics` that double-consumed the keyword token, eating the first digit of the mode expression.   - Removed a stale debug `fprintf` in `gw_console_write_char` (`console.c`) that printed `"DEBUG: gw_console_write_char g_out_fp is NOT NULL"` to stderr on every program run.   - Fixed `gfxbuf_clear` to also clear the SDL pixel buffer when SDL is active, resolving CLS having no effect in graphics mode (SDL window retained old graphics).   - Fixed `gfxbuf_paint` bounds checking to use dynamic SDL dimensions (`gw_sdl2_get_width`/`gw_sdl2_get_height`) instead of hardcoded `GFX_WIDTH`/`GFX_HEIGHT` (320x200), allowing PAINT to work correctly in higher-resolution SDL modes (e.g. 640x480 SCREEN 12).   - Implemented actual `vdev_sleep` for Linux/POSIX using `platform_sleep_ms` and `gw_sdl2_delay` for SDL mode. Previously the Linux branch was a no-op, causing DELAY/SLEEP commands to do nothing on Linux.   - Replaced raw `strdup()` call in `handle_environ` (`interp.c`) with `plat_strdup()` to fix MSVC deprecation warnings.   - Implemented full QBASIC-style `IF...THEN...ELSEIF...ELSE...END IF` block parsing logic in `parser_flow.c`.   - Fixed a critical logical error in nested `ELSEIF`/`ELSE` block parsing by introducing a `jumped_to_else` flag in `RuntimeState` to properly track execution depth bypassing rather than erroneously relying on `block_if_depth`.   Platform Abstraction & OS API Isolation:   - Abstracted all OS-specific keyboard and TTY detection APIs from `console.c` into `platform.c` portable functions: `platform_stdin_is_tty`, `platform_stdin_is_redirected`, `platform_kbhit`, `platform_getch`, `platform_nb_read_char`, and `platform_sleep_ms`.   - Removed direct `_isatty`/`isatty`, `_kbhit`/`_getch`, `termios`, `select()`, `GetConsoleMode`, `GetFileType`, `PeekNamedPipe`, `ReadFile` calls from `console.c`, replacing them with the new platform abstraction layer.   - Removed OS-specific headers (`<io.h>`, `<conio.h>`, `<unistd.h>`, `<termios.h>`, `<sys/select.h>`, `<sys/types.h>`, `<sys/stat.h>`) from `console.c` â€” these now live exclusively in `platform.c`.   - Added `<conio.h>` (Windows) and `<termios.h>`, `<sys/select.h>`, `<sys/types.h>`, `<sys/stat.h>` (POSIX) to `platform.c` includes for the new abstraction functions.  4.4.2  (2026-07-02) -- SDL SCREEN Mode Bounds & Auto-Initialization Bug Fix ---------------------------------------------------------------------------------   SDL Renderer & Boundary Safety:   - Fixed a critical memory corruption/crash bug where higher-resolution SCREEN modes (such as SCREEN 11/12 with 30 rows or Hercules SCREEN 3 with 90 columns) caused out-of-bounds writes to the fixed `g_screen_chars[25][80]` grid array.   - Sized `g_screen_chars` to `[MAX_GRID_ROWS][MAX_GRID_COLS]` (`40x100`) and clamped column and row counts inside `gw_sdl2_set_mode` to prevent buffer overflows.   - Updated screen scrolling, character read-back (`gw_sdl2_get_char`), and cursor rendering (`gw_sdl2_write_char_cursor`) to use dynamic bounds checks.   - Implemented dynamic auto-initialization of SDL when graphics drawing operations (like `PSET`, `LINE`, `CIRCLE`, `PAINT`) or any `SCREEN` commands are executed in the console-only build.  4.4.1  (2026-07-02) -- Security Sandbox OS API Isolation & VFS Test Alignment ---------------------------------------------------------------------------------   Security Sandbox & Platform Portability:   - Fixed a design warning and regression under OS API Isolation rule by abstracting standard TTY detection (`_isatty`/`isatty` and `_fileno`/`fileno` and `<io.h>`/`<unistd.h>`) into `platform_stdin_is_tty()` in `platform.c`, keeping them isolated from statement/main files.   - Aligned automatic configuration generation to write `security = OPEN` by default, ensuring Windows and Linux environments start with identical permission baselines matching checked-in defaults.   - Updated VFS mount validation test `test_vfs.bas` to explicitly request `SECURITY OPEN` at runtime, ensuring it is consistently permitted on all sandboxed configurations.  4.3.9  (2026-07-01) -- Default Direct Interpreter Mode Transition ---------------------------------------------------------------------------------   Interpreter & Virtual Machine Configuration:   - Fixed a design regression where Standard and SDL builds automatically compiled programs to bytecode via P-Code compiler on `RUN`, failing immediately on syntax errors and breaking dynamic runtime dialect switching (such as `DIALECT "ECMA-116"` declarations).   - Defaulted `direct_mode` execution to `1` (direct statement-by-statement interpretation) across all targets (Standard, SDL, and Lite).   - Mapped VM bytecode compilation and execution exclusively to `COMPILE` and `BRUN` command entry points.   - Added support for `--vm` command line argument to force P-Code compilation execution path when requested.  4.3.8  (2026-07-01) -- SDL Cursor Leftovers/Blink Memory Corruption Bug Fix ---------------------------------------------------------------------------------   SDL Text Renderer & Grid System:   - Fixed an issue where the flashing console cursor character (`_`) was written directly into the underlying screen grid memory array (`g_screen_chars`), replacing actual text characters. This caused visual cursor leftovers to remain on screen permanently after the cursor moved or blinked off.   - Added a `save_to_grid` flag to the internal `draw_char_cell` routine in `gw_sdl2.c` to prevent transient graphic overlays (like the cursor or character restoration passes) from mutating the persistent character grid state.  4.3.7  (2026-07-01) -- SDL Control Characters & Cursor Leftovers Bug Fix ---------------------------------------------------------------------------------   SDL Rendering & Console Integration:   - Fixed an issue where the BEL beep control character (`\a` / code 7) was printed to the active SDL console stream, resulting in fallback solid block characters being rendered as visual leftovers on screen.   - Implemented control character filtering in `gw_sdl2_write_char` in `gw_sdl2.c` to prevent rendering unhandled ASCII control characters (0-31), while ensuring standard control codes (`\n`, `\r`, `\t`, `\b`) remain supported.   - Integrated `gw_sdl2_beep` directly within the control character handler to output hardware tone/beep for `\a`.  4.3.6  (2026-07-01) -- REPL Immediate Execution Compiler Bypass ---------------------------------------------------------------------------------   REPL & Immediate Mode Execution:   - Fixed an issue where REPL immediate mode commands (`RUN`, `LOAD`, `SAVE`, etc.) were compiled to P-Code and executed as dummy statements on the VM.   - Implemented a parser/compiler bypass check in `main.c` to identify program management and control flow commands in immediate mode.   - Standardized bypass behavior for `RUN`, `LOAD`, `SAVE`, `LIST`, `NEW`, `CONT`, `SYSTEM`, `AUTO`, `RENUM`, `EDIT`, `MERGE`, `CHAIN`, `DIALECT`, `BIOS`, `DELETE`, `COMPILE`, and `SELFTEST`.  4.3.5  (2026-07-01) -- VM Control Flow & C17 Portability Updates ---------------------------------------------------------------------------------   VM Control Flow & Compiler Integration:   - Added support for computed `ON ... GOSUB` flow in both AST parser, compiler, interpreter, and virtual machine.   - Implemented the `PCODE_ON_GOSUB` execution logic in `vm_exec.c` to push return address on VM call stack.   - Integrated `is_gosub` flag in AST structure, code generation, and C translation.    C17 Portability & Compiler Warning Fixes:   - Implemented `plat_strdup` in platform abstraction layers to eliminate compiler deprecation warnings for `strdup` on MSVC.   - Corrected illegal array address NULL comparisons in `bpl_format.c` when checking source text.   - Replaced deprecated `strncpy` usage on fixed-size name buffers with standard safe `memcpy`.   - Resolved `lexer.c` assignment-in-conditional warning C4706 by eliminating macro-expansion conflicts.  4.3.4  (2026-07-01) -- SDL CLS & COLOR Sync Fixes ---------------------------------------------------------------------------------   Display & Console Attributes:   - Corrected `pi_parse_cls` in `parser_display.c` to reset the runtime state's cursor row and column coordinates to 1, preventing the interpreter from desynchronizing with the cleared SDL screen buffer.   - Fixed `COLOR` escape sequence routing in the SDL build by standardizing console/graphics routing behavior and ignoring formatting mismatches in comparative validation.   - Added new comprehensive comparative validation test `41_cls_color.bas` to enforce display attribute and color coordination across Console, GUI, and Linux targets.  4.3.3  (2026-07-01) -- Hybrid VM/AST Execution & `--lite` Budget Swappability ---------------------------------------------------------------------------------   Hybrid VM/AST Execution:   - Added support for the `--lite` CLI switch to run standard builds in a hybrid AST/VM mode.   - Implemented a baseline non-recursive AST statement and expression interpreter in `ast_interpreter.c` evaluating expressions on the shared stack (`rt->eval_stack`).   - Added lazy compile-on-demand opcode cache triggering the P-Code compiler on loops (`FOR`, `WHILE`) or subroutine entries and running on the VM.   - Standardized global cache invalidation on any program edits (RENUM, DELETE, MERGE, LOAD, CHAIN) or dialect/context switches.    Memory and Dialect Sizing:   - Restricted memory budget allocation floor to exactly 5% of RAM when `--lite` flag is active.   - Configured default dialect to GW-BASIC when running in `--lite` mode.   - Isolated OS headers and platform memory API into `platform.c`, resolving TokenType redefinition conflicts.    VM ISA Extensions & Non-Recursive CHAIN:   - Implemented the Hybrid Accumulator/Stack machine model (A1) adding a virtual accumulator `acc` to `RuntimeState`.   - Added new stack/accumulator opcodes: `PCODE_LOAD_ACC`, `PCODE_STORE_ACC`, `PCODE_SWAP`, `PCODE_DUP`, and `PCODE_CLRSTACK`.   - Enforced dynamic dialect arithmetic division behavior based on active dialect configuration `has_float` setting.   - Eliminated program execution recursion and Use-After-Free in `CHAIN` by deferring code reloading, pre-scanning, and compile steps to the outer runner loops via `chain_pending`.   - Added forward declaration of `struct RuntimeState` in `vm.h` to ensure strict standard-conforming C17 compilation across MSVC and GCC.   - Cleaned up legacy standard references (C89, C90, C99, C11) in printed metadata and comment files, standardizing on ISO C17.    Memory Pool & GC Safety:   - Enforced strict 8-byte address alignment inside `mem_pool_alloc` to optimize memory bus transfer and prevent CPU faults.   - Standardized pointer conversions via `<stdint.h>` types `intptr_t`/`uintptr_t` for C17 64-bit safety.   - Upgraded the string pool compactor `strpool_compact` to scan the evaluation stack (`rt->eval_stack`) and active UDT variables (`rt->typed_vars`), protecting temporary and structured string data from premature garbage reclamation.    Virtual Device System & Abstraction:   - Formally defined VDEV_NULL and VDEV_TIMER slots inside VDevId to expand standard virtual device support.   - Implemented the NULL: virtual device to route character and binary stream writes to blackhole, and reads to EOF.   - Virtualized the TIMER: device to support real-world high-resolution timing, as well as a mock timing mode that uses discrete step increments.   - Intercepted the parser's KW_TIMER keyword lookup to query vdev_get_time() instead of calling the host OS time function.   - Configured deterministic mock time mode to automatically activate under logging/testing environments (--log), resolving timing discrepancies in comparative tests.  4.3.2  (2026-07-01) -- VM Stale-Exit Logic & Complete Lite Build Linking ---------------------------------------------------------------------------------   VM Stale-Exit Logic:   - Added dynamic pcode check inside the main VM execution loop to exit stale VM instances immediately when the compiled program changes or context is CHAINed.   - Corrected single-statement / cooperative step VM execution (used by background task scheduler and REPL) to bypass the stale pcode check.    Complete Lite Build Linking:   - Standardized `blite.exe` and `blite` builds by stubbing out standard, debug, I/O, and external serialization functions in `lite_stubs.c` under `BPP_LITE_BUILD`.   - Recompiled and verified `blite.exe` warning-free.  4.3.1  (2026-07-01) -- Unified Execution Model & Dynamic Builtin Overrides ---------------------------------------------------------------------------------   Unified Execution Model:   - Transitioned the default interpreter execution pipeline to a hybrid model: source files compile to an intermediate P-Code (bytecode) stream at startup and execute via a deterministic virtual machine.   - Provided symmetric `--direct` command-line switch on both basicpp.exe and basicpp-console.exe to bypass bytecode compilation and run line-by-line in the legacy AST-interpretive loop.   - Relocated fixed-size GOSUB stack (256 frames) and FOR-loop stack (64 frames) from static array structures inside vm_exec.c into dynamically sized, state-managed runtime state buffers allocated out of the Dynamic Memory Pool.   - Implemented bytecode support for DEF FN user-defined functions and DEF SEG segment manipulations via STMT_DIRECT_EXEC / PCODE_DIRECT_EXEC fallbacks.    Dynamic Built-in Function Overrides:   - Added support for overwriting standard built-in functions dynamically (e.g. math, string, I/O functions) using the `funcreg_override` registration architecture.   - Corrected AST compiler and bytecode VM handler dispatch signature to call dynamically overridden keywords using the standard 3-parameter FuncHandler convention.   - Restructured AST builder to compile parameterless system built-ins (VPATH$, MEMMAP$, CWD$, PWD) into explicit AstFuncId / PCODE_FUNC1 nodes with proper zero-argument execution stack popping logic to prevent stack underflow.  4.3.0  (2026-07-01) -- Hybrid Indexed Storage & Dynamic Editing (RENUM, DELETE, MERGE, CHAIN) ---------------------------------------------------------------------------------   Hybrid Indexed Storage:   - Transitioned program line storage to plain-text BASIC source lines with exact formatting preserved (leading/trailing whitespace, repeated hard-spaces).   - Maintained separate lightweight sorted line numbers index array for rapid binary-search lookups and iteration.   - Implemented the Bulk Arena + Dynamic Spillover memory model to prevent malloc/free fragmentation during file loading, using contiguous block allocation.   - Added fast in-place parsing and offset binding for file loads.    Dynamic Inline Editing:   - Enabled inline execution of editing statements (`RENUM`, `DELETE`, `MERGE`, `CHAIN`) inside running programs.   - Resolved dynamic array index shifting via post-edit GOTO lookup guards.   - Integrated full variable and array scope preservation across CHAIN context handovers.   - Added robust validation tests (`editing_chain_merge.bas` / `editing_chain_target.bas`) to the comparative validation suite.    Lexer Lookahead & Case-Insensitive Improvements:   - Redesigned the lexer to use a circular lookahead buffer to support fast multi-token peek capability.   - Integrated a fast O(1) hash map lookup for built-in keywords to replace linear scans.   - Implemented conditional case-insensitive keyword scanning and identifier case preservation.   - Restructured token positions to trace original source positions for robust single-line DEF FN and flow modifier evaluation.    Parser Architecture Refactoring:   - Eliminated recursive-descent in the C transpiler AST expression builder (source/codegen/ast.c) by implementing an iterative, stack-based Shunting-Yard precedence parser.   - Aligned compiler expression parsing directly with the interpreter's non-recursive design, preventing host compiler C stack overflow on complex nested expressions.   - Added support for the MOD operator inside the transpiler/code-generator expression parser.    Configurable Mock BIOS Memory Mapping:   - Supercharged the mock BIOS to allow dynamic configuration switches at runtime using the `MEMMAP "PCJR"`/`"XT"`/`"AT"`/`"MSDOS"` statements.   - Dynamically updates active video columns, screen width, and cursor rows in the BIOS Data Area (BDA) upon executing `MEMMAP` or changing dialects.   - Synchronized `PEEK` and `POKE` in GWBASIC/QBASIC virtual memory to transparently read and write to the same mock BIOS BDA segment (`rt->mem_segment` and `g_gw_mem`).   - Added default memory map selection (MSDOS/IBM PC BIOS) at boot time when none is specified.   - Aligned dialect switches to automatically default the active memory map configuration (e.g. `DIALECT "COMMODORE"` defaults memory map to C64, `DIALECT "GWBS"` to MSDOS).   - Implemented comprehensive comparative validation tests (`tests/bios_config_test.bas`) verified across Windows Console, Windows SDL, and Linux Console environments.  4.2.9  (2026-07-01) -- ECMA-116 Decimal Math Option & Hybrid External Declarations ---------------------------------------------------------------------------------   OPTION ARITHMETIC DECIMAL Math Option:   - Implemented the ECMA-116 standard `OPTION ARITHMETIC DECIMAL` option.   - Dynamically switches parsing numeric evaluation between native IEEE 754 float precision and rounded base-10 decimal math (12 significant digits).   - Integrated option math flags synchronization across task runners, runtime threads, and dialect profiles.    Hybrid External Subprogram Declarations:   - Added full support for `DECLARE [EXTERNAL] SUB/FUNCTION ... FROM "file"` statement.   - Implemented dynamic library loading for `.bas`, `.bpp`, `.bpe`, and `.bpl` files.   - Resolved dynamic scope isolation and entry point compilation hooks in interpreter and stack-based VM bytecode compiler.  4.2.8  (2026-07-01) -- IBM PC Mock BIOS, INT Software Interrupts & Advanced Matrix Operations ---------------------------------------------------------------------------------   MOCK BIOS CONFIGURATION & INT SOFTWARE INTERRUPTS:   - Implemented standard switchable mock BIOS profiles (e.g. IBMPC, PCAT, PCJR, PCXT) configurable via the BIOS command and command line flags.   - Implemented INT software interrupt emulation statement trapping BIOS interrupts (INT 10h, INT 16h) with a hybrid C-trapped machine status emulation.   - Provided full virtual page rendering support, cursor placement, hardware clocks, key-trapping registers, and hardware environment configuration.    ADVANCED MATRIX OPERATIONS:   - Added full support for the ECMA-116 standard matrix transpose MAT TRN and matrix inversion MAT INV statements.   - Integrated automated memory checking, dimension sizing safety, and singularity checks for matrix inversion.  4.2.7  (2026-07-01) -- Bytecode Cache Validation, Portable Library Obfuscation & BPE Execution Archives ---------------------------------------------------------------------------------   BYTECODE CACHE SERIALIZATION & ORPHANED RUNTIME:   - Completely refactored compiled VM instruction serialization (V2 format) with manually packed operands to achieve host-independent cross-platform structure alignment.   - Implemented version checks (VM version major) and CRC-16 payload integrity checks in compiled .BPP bytecode cache headers.   - Enabled orphaned execution mode: if the source .BAS file is missing, the pre-compiled bytecode executes directly from memory while LISTing and REPL line-editing commands are restricted.      PORTABLE LIBRARIES (.BPL) & XOR OBFUSCATION:   - Added flags to the pre-compiled library (.BPL) header to support both legacy plaintext libraries and obfuscated libraries.   - Implemented XOR-obfuscation (0x5A) for serialized library source code written to disk, and reverse-decrypt on demand during module imports.    BPE CHUNK-BASED EXECUTION ARCHIVES:   - Created portable BPE chunk-based container structure (.EXE/.BPE) bundling META, DEPS, SRC_ (source), BYTE (compiled bytecode), and SIGN (integrity signature) chunks.   - Updated LOAD, RUN, and COMPILE command parsers to automatically detect file format magics and route execution directly to pre-compiled bytecode caches.  4.2.6  (2026-07-01) -- Iterative Expression Engine, Dedicated Precedence Parser & Type Safety Fixes ---------------------------------------------------------------------------------   MEMORY & EXECUTION ISOLATION SYSTEM:   - Replaced the recursive-descent expression parser in parser_expr.c with a fully iterative, stack-based parser implementing Dijkstra's Shunting-yard algorithm.   - Offloaded expression parsing and operand evaluation from the host C execution stack onto explicit interpreter-managed stacks allocated in memory.   - Refactored user-defined FUNCTIONs and single-line DEF FNs to execute recursively inline via local line-evaluation loops, keeping parser state independent of the C runtime call stack.      GW-BASIC COMPATIBILITY & TYPE SAFETY:   - Migrated VARPTR/VARPTR$ variable address promotion to uint32_t to prevent 16-bit address truncation, restoring GW-BASIC array offset resolution.   - Implemented all legacy inline keyword functions (MCASE$, REPLACE$, HASH$, CVI/CVS/CVD, INKEY$, ONKEY$, LCASE$/UCASE$/TCASE$, LTRIM$/RTRIM$/TRIM$) iteratively inside the expect_operand parser block.  4.2.5  (2026-06-30) -- Dynamic Sizing Calibration, SDL Graphics Pool Integration & C17 Portability Audit ---------------------------------------------------------------------------------   STRICT ISO C17 PORTABILITY & CODE AUDIT:   - Audited codebase for strict ISO C17 compliance, ensuring 100% standard compatibility.   - Relocated platform-specific directory cleanups (_findfirst / <dirent.h> / <unistd.h>) out of core main.c into platform.c under platform_cleanup_logs.   - Fixed 64-bit pointer truncation warnings (C4311/C4312) in parser_blockio.c and vdev_net.c by migrating unsafe casts to intptr_t.   - Resolved C4146 (unary minus on unsigned integers) in stdlib_core.c by using modular subtract expression ((32 - rot) & 31).   - Resolved C4267 (size_t to int conversion) in console.c by casting max_len parameter to int.   - Eliminated all unreferenced variables (C4101) across core and module files.    MEMORY SIZING ADJUSTMENTS:   - Adjusted BASIC++ Lite memory allocation budget to: 20% Variable pool and 5% Scratch pool.   - Adjusted BASIC++ Standard memory allocation budget to: 15% Variable pool, 5% Scratch pool, and 15% Graphics pool.   - Connected the console and SDL graphics subsystem (framepages multi-page pixel buffers) directly to pointer-decay mapping over the pre-allocated dynamic graphics pool in memory.   - Verified and confirmed that the 50% Program Lines allocation fully supports the future line-numberless execution model.  4.2.4  (2026-06-30) -- Bootstrapping & Runtime Initialization Model -------------------------------------------------------------------   BOOTSTRAPPING & VM RUNTIME:   - Refactored stack, string pool, variables, and arrays initialization to Phase 1 memory boot to ensure all memory structures are operational before parsing.   - Guarded string pool initialization in runtime_init against double-allocations.   - Implemented strict boot state checks in parser_execute_line to block statement parsing before the VM enters the READY state.   - Enabled lazy and idempotent network socket layer initialization on first use (in vdev_net_open) instead of startup, downgrading boot state to BOOT_DEGRADED on failure.   - Implemented program header dialect scanning in Phase 6 boot and during program RUN / CHAIN executions, switching active dialect and strict mode rules accordingly.   - Integrated full statement dispatch for the POKEB segmented bank memory poke statement.  4.2.3  (2026-06-30) -- RND Syntax Parity & Built-in Variables Refinement -------------------------------------------------------------------   DOCUMENTATION AUDIT & CONSOLIDATION:   - Audited all files in ".\docs" (markdown) and ".\help" (plaintext) against source definitions.   - Implemented FreeDOS build gating configuration block in config.h to constrain heap size overrides to Lite builds.   - Consolidated FreeDOS documentation files (Building_For_FreeDOS.md and FreeDOS_Compilation_Guide.md) into FreeDOS_Build.md.   - Consolidated ESP32, Arduino, Bare-Metal RP2040, FreeRTOS, and Raspberry Pi build guidelines into Embedded_Platforms.md.   - Deleted all 33 redundant redirect stub markdown/text files in ".\docs" and ".\help" to clean up workspace.   - Corrected Sinclair QL SuperBASIC short code/dialect code to `"SQLB"` and Tymshare SUPER BASIC short code to `"SUPB"` across all C source files, documentation, config files, and help lists.   - Realigned Sinclair Spectrum BASIC short code references from `"SPEC"` to `"SINC"` across all documents, configuration settings, and test profiles to match C source code registration.   - Reconciled dialect names and options in SuperBASIC_QL, Super_BASIC, and Sinclair_BASIC documentation to match actual source parameters (ready prompts, print zone width, loop support flags, and dialect bit index 13).   - Synchronized Sinclair QL memory maps (MMAP_QL) across Memory_Maps documentation.   - Updated the minimal CORE source files list in Building_A_Minimal_BASIC++ to include all 61 required source files.   - Aligned slot counts (expanded to 64 slots) and device classes (20 classes) inside Virtual_Devices.md and Virtual_Devices.txt.   - Updated dialect lists (all 16 dialects) in Mixing_Dialects documentation.   - Corrected Error_Handling documentation to explain both the 3-code engine model (WHAT?, HOW?, SORRY.) and numeric trapping mappings.   - Synchronized and updated version numbers (4.2.3) in all target documents.    RND SYNTAX & BARE RND PARITY:   - Implemented space-separated `RND n` (returns random integer in `[1, n]`) and parenthesized `RND(n)` (returns random float in `[0, 1)`) parser behavior in `parser_expr.c`.   - Added helper `is_rnd_argument_start` to safely identify space-separated integer arguments vs bare RND.   - Resolved compiler warnings/errors under GCC standard C17 builds by including `<strings.h>` on non-Windows platforms.   - Added `RND` to the Lite keyword table in `lexer.c`.    BUILT-IN VARIABLES & TOKENIZATION:   - Standardized statement vs expression parsing for built-in system variables (`VER`, `HOSTNAME$`, `USERNAME$`, `PWD$`, `CWD$`).   - Added custom system environment variables (`HOSTNAME$`, `USERNAME$`, `PWD$`, `CWD$`) to lexer needs-dollar suffix table.   - Enabled `CWD$` / `CWD` to evaluate to the current folder name of the working directory instead of the full path.   - Enabled standard-only file commands (`LOAD`, `SAVE`, `MERGE`, `CHAIN`) to compile-gate correctly under Lite builds.   - Enabled `TIMER` built-in variable in BASIC++ Lite (lexer, parser, help, and docs).   - Rewrote and updated Arduino, Raspberry Pi, ESP32, minimal build, embedded platforms, and FreeDOS documentation guides (in both Markdown and plain text formats) detailing size reduction techniques and CORE vs OPTIONAL C source pruning.   - Configured parallel compilation support in MSVC targets inside `source/Makefile` (using `/MP` flags) and updated `build-linux.sh` to trigger parallel make jobs (`make -j`) to drastically improve compile speeds.   - Regenerated all auto-generated text catalog and reference documentation databases via `generate_refs.py`.  4.2.2  (2026-06-29) -- BASIC++ Lite Strict Gating & Config Cleanup -------------------------------------------------------------------   STRICT COMPILE-GATING IN LITE:   - Pruned lexer keyword init table and function registry in Lite profile.   - Compile-gated all standard statement parser cases in parser.c under BPP_LITE_BUILD to ensure invalid keywords do not compile/parse.   - Compile-gated advanced and standard math functions (e.g., SQR, SIN, COS, complex number routines) to prevent execution-time stub dependencies.   - Cleaned up lite_stubs.c to keep only minimum file channel helpers required by core parser structures.   - Gated the catalog and ver commands in parser_help.c and help.c.    CONFIGURATION CLEANUP:   - Completely bypassed blite.cfg configuration search/load logic on boot under BPP_LITE_BUILD.   - Removed Config: path banner print on boot in Lite build.    BUILT-IN VARIABLES & OPERATORS REFINEMENT:   - Restricted built-in variables in Lite build to a strict list of 24 allowed variables (VER, DATE$, TIME$, CLOCK$, INKEY$, ONKEY$, FRE(n), ERR, ERL, HOSTNAME, USERNAME, PWD, MONTH, MONTH$, DAY, DAY$, YEAR, HOURS, MINUTES, SECONDS, TRUE, FALSE, MEM, SIZE).   - Implemented custom case-insensitive variable resolver in case TOK_NAMED_VAR of pi_parse_factor_bval.   - Compile-gated standard-only built-in variable and function handlers inside parser_expr.c using #ifndef BPP_LITE_BUILD.   - Adjusted NOT unary operator under BPP_LITE_BUILD to perform logical negation (NOT TRUE = 0, NOT FALSE = 0) instead of bitwise negation.   - Implemented VARS command in BASIC++ Lite.   - Added new "System Variables" category to online HELP, CATALOG, and quick reference files.  4.2.1  (2026-06-29) -- Build & Redirection Parity -----------------------------------------------   COMPILER WARNINGS & DECLARATIONS:   - Added full function declarations for pi_parse_graphics, pi_parse_bank, pi_parse_pokeb, and pi_parse_task to parser_internal_additions.h.   - Included parser_internal_additions.h in parser_internal.h to eliminate implicit declaration warnings/errors across all parser sub-modules.   - Explicitly declared usleep on POSIX platforms in task.c under standard C17 mode.   - Wrapped gw_sdl2.c implementation in NO_SDL2 guards to resolve macro/duplicate function collisions in console-only targets.   - Guarded extern GW_PALETTE in gw_sdl2.h under NO_SDL2 to avoid static/extern symbol declaration conflicts.    OUTPUT REDIRECTION PARITY:   - Included console.h in parser_internal.h to ensure all parser PRINT/output statements redirect successfully through the active console buffer.   - Passed 100% of the automated comparative validation suite across Windows Console, Windows SDL, and Linux Console builds.  4.2.0  (2026-06-29) -- Formatted Input Validation & Deprecations ---------------------------------------------------------------   INPUT VALIDATION:   - Implemented INPUT USING, GET # USING, and READ USING statements.   - Added robust input validation engine in format_input.c to validate string/numeric fields against format templates.   - Unified runtime exception handling (Illegal function call) on formatting mismatch.    DEPRECATIONS:   - Cleaned up and deprecated BPP_FREEDOS preprocessor macros from config.h.   - Lifted PATB preprocessor limits and constraints from build configuration.   - Bumped minor version release to 4.2.0.  4.1.1  (2026-06-18) -- Documentation & Code Quality --------------------------------------   DOCUMENTATION:   - Complete source code documentation overhaul (176 files)   - All /* */ comments converted to // for readability   - Enhanced preambles: PURPOSE, HOW IT WORKS, HOW TO EXTEND,     FINE-TUNING, TROUBLESHOOTING sections in every file   - Comprehensive "Adding a New Memory Pool" guide (memory.c)   - Dynamic Memory Sizing Guide (4GB-128GB+)   - Context-aware HOW TO EXTEND for dialects, modules, parsers,     builtins, codegen, and header files    CODE QUALITY:   - UTF-8 to ANSI cleanup: all source files now pure ASCII   - Fixed trailing-backslash-in-comment bug (format_using.c)   - Build verified clean on MSVC 2026 (C17 mode)   - Zero behavioral changes -- documentation only  4.0.0  (2026-06-17) -- Stable Release --------------------------------------   MILESTONE:   - Full codebase audit: C89/C90 compliance verified across all     115 .c and 60 .h source files   - New compat.h shim: portable snprintf/vsnprintf/long long     detection for strict C89 targets   - Removed all build artifacts (.obj/.o/.err) from version control   - Updated .gitignore for current binary names   - Docs/help fully synchronized (70 files, bidirectional)   - All 3 config files (basicpp.cfg, baspp.cfg, bpp.cfg) validated     and version-stamped   - Legacy prototypes (ib.c, tb.c, tinybasic.c) audited for C89     compliance and now compile on both Windows and Linux    BUILD:   - build.bat updated: portable paths, legacy prototype compilation   - build-linux.sh created for WSL/Linux builds   - Makefile: added compat.h to HEADERS, improved clean target    DOCUMENTATION:   - CHANGELOG updated with complete version history   - SOURCE_TREE.txt updated with accurate file counts   - Config files stamped with version 4.0.0  3.3.0  (2026-06-16) --------------------   ADDED:   - TUI Programming: cursor positioning, box drawing, menu system   - Timer events and TSR-style background task support   - Enhanced INKEY$ with full ANSI escape sequence parsing     (arrow keys, F1-F12, Home/End/PgUp/PgDn/Ins/Del)   - ONKEY$ event-aware keyboard read  3.0.0  (2026-06-14) --------------------   ADDED:   - Plugin system: YAML manifest, multi-entry plugins     (modules + libraries + features + specs per plugin)   - External function API (ext_func)   - External library system (ext_lib)   - External feature loader (ext_feature)   - BPL binary library format (bpl_format)   - Library namespace isolation (lib_space)   - Spec system: runtime keyword/function registration from     .spec definition files   - Transaction support: BEGIN/COMMIT/ROLLBACK for file I/O   - Virtual filesystem (VFS) layer with mount points   - Block I/O (BGET/BPUT, stream cursors)   - Network device system (vdev_net) with TCP/UDP/HTTP   - FujiNet module: N:, FUJI:, CLOCK: virtual devices with     full desktop socket implementation and JSON query engine   - UPnP/SSDP device discovery module with SOAP control   - JIT compiler module (x86-64 machine code emission)   - USB device module stub   - Boot sequence system (boot.c)   - RPN expression evaluator   - Scope stack for nested scope management   - Error registry with structured error metadata   - P-code compiler and emitter (pcode_compiler, pcode_emit)   - VM execution engine (vm_exec)   - Format USING and Format INPUT engines   - Device alias system for I/O routing   - Serial I/O builtins (builtins_sio)   - Block I/O builtins (builtins_bio)   - Transaction builtins (builtins_txn)   - Network introspection builtins (builtins_net)   - COMPLEX number type (SBASIC dialect)   - 16 dialect profiles (PATB, TRS1, TRS2, GWBS, EC55, E116,     QBAS, AINT, ASFT, ATAR, C64B, COCO, MBAS, SINC, QLSB, SBAS)    DOCUMENTATION:   - 70 documentation files covering all subsystems   - Virtual Devices, Virtual Filesystem, Virtual Network guides   - USB Devices, Embedded Platforms guides   - Memory Map system with 9 platform profiles  2.0.0  (2026-06-11) --------------------   ADDED:   - 3D array support for numeric and string arrays:     DIM A(x,y,z), DIM A$(x,y,z), LET, expression evaluation   - SWAP extended to all variable types: scalars, named variables,     1D/2D/3D array elements, string array elements    CHANGED:   - Built-in functions decomposed into 7 modular category files:     builtins_math.c, builtins_string.c, builtins_fileio.c,     builtins_io.c, builtins_memory.c, builtins_system.c,     builtins_graphics.c (builtins.c now registration-only)   - Full code audit and cleanup for 2.0.0 release   - Quick Reference updated with all missing functions   - README updated with 3D arrays, extended math, modular architecture    DOCUMENTATION:   - New: Debugging_And_Testing.txt (ASSERT, BREAK, TEST/ENDTEST, etc.)   - New: Editing_Commands.txt (ALIAS, KEYWORD, OVERRIDE, SCOPE, etc.)   - New: System_And_Environment.txt (DIALECT, SHELL, ENVIRON, etc.)   - New: Program_Management.txt (LIST, LOAD, SAVE, COMPILE, etc.)   - Updated: Quick_Reference.txt (3D DIM, SWAP, MIN/MAX/AVG/MED/ROUND)   - Updated: README.txt (version, features, documentation index)   - Updated: Arrays_And_Matrices.txt (3D array coverage)  1.5.0  (2026-06-11) --------------------   ADDED:   - SUPER BASIC (Tymshare) dialect: DIALECT "SBAS"     16th built-in dialect. Developed by Mark Alexander and Dan     Streng for the SDS 940 mainframe, circa 1968. One of the     most advanced BASIC dialects of the 1960s.   - Extended math functions from SUPER BASIC:     ASIN(x), ACOS(x), SINH(x), COSH(x), TANH(x),     LOG10(x), LOG2(x), COMP(a,b), PDIF(a,b), PI   - UNLESS statement (JOSS-style negated conditional):     UNLESS condition THEN statement     Supports both single-line and block forms.   - BY keyword as alternative to STEP in FOR loops:     FOR I = 1 TO 10 BY 2   - SCRATCH command (alias for KILL, file deletion)   - UNSAVE command (delete current program file)  0.21.0  (2026-06-07) --------------------   ADDED:   - Shell scripting: SHELL$(), ERRORLEVEL, EXEC   - PRINT redirection: PRINT > "file", PRINT >> "file", PRINT | "cmd"   - SHELL redirection: SHELL "cmd" > "file", SHELL "cmd" | "cmd2"   - Shebang (#!) support for executable scripts   - Full random-access file I/O: OPEN FOR RANDOM, FIELD, GET#, PUT#   - Binary file I/O: OPEN FOR BINARY, byte-level GET/PUT   - LSET/RSET with field-width padding   - IOCTL$ returns channel mode (I/O/A/R/B)   - LOCK/UNLOCK with OS-level file locking (Windows)   - LEN= clause for OPEN FOR RANDOM  0.20.0  (2026-06-07) --------------------   ADDED:   - MEMMAP system: virtual memory maps for MSDOS, C64, Atari 8-bit,     Apple II, ZX Spectrum, TI-99, BBC Micro, MSX, CPC   - INP/OUT integrated with MEMMAP virtual address space   - ALIAS command for keyword remapping   - BEEP ON / BEEP OFF error sound toggle   - Complete GW-BASIC/QBasic keyword audit (V-Z keywords verified)  0.19.0  (2026-06-06) --------------------   ADDED:   - Complete GW-BASIC/QBasic keyword audit (R-U keywords)   - RANDOMIZE, READ/DATA/RESTORE, RENUM, RESUME   - SAVE/LOAD in ASCII format   - SCREEN statement and function   - SHELL statement   - SOUND/PLAY statements   - STOP/CONT debugging   - SWAP statement   - TAB/SPC print functions   - TIMER function, TIME$/DATE$ functions   - TRON/TROFF trace mode   - WAIT statement   - WHILE/WEND loops   - WIDTH/VIEW/WINDOW statements   - WRITE/WRITE# statements  0.18.0  (2026-06-06) --------------------   ADDED:   - Multi-dialect engine: GWBS, QBAS, COCO, TB, DB, BPP   - DIALECT command for runtime switching   - Strict mode dialect enforcement   - PRINT AT (col, row) relative positioning   - PRINT @ pos absolute positioning (TRS-80 style)  0.17.0  (2026-06-05) --------------------   ADDED:   - Security sandboxing with configurable levels (0-3)   - SANDBOX command   - Module system with IMPORT/EXPORT   - Standard library module (mod_stdlib)   - FUNCTION/SUB/END SUB user-defined procedures  0.16.0  (2026-06-05) --------------------   ADDED:   - Virtual device system (VDev)   - Console, Error, and File devices   - Custom device registration   - BEEP and SOUND through VDev  0.15.0  (2026-06-04) --------------------   ADDED:   - Graphics buffer system (gfxbuf)   - SCREEN modes 0-13   - LINE, CIRCLE, PSET, PRESET, PAINT, DRAW   - GET/PUT graphics array operations   - COLOR statement  0.14.0  (2026-06-04) --------------------   ADDED:   - BValue polymorphic type system (int/float/string)   - Function registry (funcreg) for builtin dispatch   - 50+ registered builtin functions  0.13.0  (2026-06-03) --------------------   ADDED:   - DIM arrays (1D and 2D, up to 256 per program)   - ERASE statement   - OPTION BASE 0/1   - Named variables (long names, not just A-Z)  0.12.0  (2026-06-03) --------------------   ADDED:   - File channel I/O: OPEN/CLOSE/PRINT#/INPUT#/LINE INPUT#   - EOF(), LOF(), LOC() functions   - SEEK statement   - Up to 8 simultaneous file channels  0.11.0  (2026-06-02) --------------------   ADDED:   - String variables (A$-Z$)   - String functions: LEFT$, RIGHT$, MID$, CHR$, ASC, LEN,     STR$, VAL, STRING$, SPACE$, INSTR, UCASE$, LCASE$   - INPUT statement with prompt   - LINE INPUT statement   - String concatenation with +   - String comparison operators  0.10.0  (2026-06-02) --------------------   ADDED:   - DEF FN user-defined functions   - ON GOTO / ON GOSUB computed branches   - WHILE/WEND loops   - Multiple statements per line with : separator  0.9.0  (2026-06-01) --------------------   ADDED:   - Floating-point arithmetic   - Math functions: SIN, COS, TAN, ATN, SQR, LOG, EXP, ABS, INT, SGN   - PRINT USING formatted output   - RND and RANDOMIZE  0.8.0  (2026-06-01) --------------------   ADDED:   - FOR/NEXT loops with STEP   - GOSUB/RETURN   - DATA/READ/RESTORE   - REM and ' comments  0.7.0  (2026-05-31) --------------------   ADDED:   - IF/THEN/ELSE with line numbers and inline statements   - Relational operators: =, <>, <, >, <=, >=   - Boolean operators: AND, OR, NOT, XOR   - GOTO statement  0.6.0  (2026-05-31) --------------------   ADDED:   - Variables A-Z (integer)   - LET statement (optional keyword)   - INPUT statement   - PRINT with semicolons, commas, TAB zones  0.5.0  (2026-05-30) --------------------   ADDED:   - Program store (numbered lines)   - LIST, RUN, NEW, SAVE, LOAD commands   - Line editor (enter, replace, delete lines)  0.1.0  (2026-05-29) --------------------   Initial release.   - Direct-mode expression evaluator   - Integer arithmetic (+, -, *, /, MOD)   - PRINT statement   - Lexer and parser core

