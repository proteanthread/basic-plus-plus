#ifdef _MSC_VER
#ifndef _CRT_SECURE_NO_WARNINGS
#define _CRT_SECURE_NO_WARNINGS
#endif
#pragma warning(disable: 4996)
#pragma warning(disable: 4101)
#pragma warning(disable: 4244)
#pragma warning(disable: 4102)
#pragma warning(disable: 4311)
#pragma warning(disable: 4312)
#endif

#ifdef __GNUC__
#pragma GCC diagnostic ignored "-Wunused-function"
#pragma GCC diagnostic ignored "-Wunused-variable"
#pragma GCC diagnostic ignored "-Wunused-label"
#pragma GCC diagnostic ignored "-Wstringop-truncation"
#pragma GCC diagnostic ignored "-Wrestrict"
#pragma GCC diagnostic ignored "-Wdouble-promotion"
#pragma GCC diagnostic ignored "-Wpointer-to-int-cast"
#pragma GCC diagnostic ignored "-Wint-to-pointer-cast"
#endif

/* Generated by BASIC++ Compiler for target: windows */
#define BPP_TARGET_WINDOWS
#define BPP_SUPPORT_MULTITASKING 1
#define BPP_SUPPORT_GRAPHICS 1
#define BPP_SUPPORT_FILESYSTEM 1

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

/* Windows specific platform headers */
#include <windows.h>

/* === Runtime Variables === */
static double bpp_vars[1024]; /* A-Z and named */
static char bpp_strvars[1024][256]; /* A$-Z$ and named */
static double bpp_at_array[4096]; /* @() legacy array */
static int bpp_print_width = 14;

/* === GOSUB Stack === */
#define BPP_MAX_STACK 256
static int bpp_gosub_stack[BPP_MAX_STACK];
static int bpp_gosub_sp = 0;

/* === DIM Arrays === */
#define BPP_MAX_DIM 64
#define BPP_MAX_ELEM 4194304
static double bpp_dim_pool[BPP_MAX_ELEM];
static int bpp_dim_used = 0;
typedef struct { char name[32]; int d1,d2,off; } BppDim;
static BppDim bpp_dims[BPP_MAX_DIM];
static int bpp_dim_count = 0;

static void bpp_dim_alloc(const char *nm, int d1, int d2) {
 int sz = (d1+1) * (d2>0 ? d2+1 : 1);
 BppDim *d = &bpp_dims[bpp_dim_count++];
 strncpy(d->name, nm, 31); d->name[31]='\0';
 d->d1=d1; d->d2=d2; d->off=bpp_dim_used;
 memset(&bpp_dim_pool[bpp_dim_used], 0, sizeof(double)*(size_t)sz);
 bpp_dim_used += sz;
}

static double *bpp_dim_ref(const char *nm, int i1, int i2) {
 int k;
 for (k=0; k<bpp_dim_count; k++) {
 if (strcmp(bpp_dims[k].name, nm)==0) {
 int idx = (bpp_dims[k].d2>0)
 ? i1*(bpp_dims[k].d2+1)+i2 : i1;
 return &bpp_dim_pool[bpp_dims[k].off + idx];
 }
 }
 fprintf(stderr, "Array not found: %s\n", nm);
 exit(1);
 return NULL;
}

/* === String DIM arrays === */
#define BPP_MAX_STRDIM 26
#define BPP_MAX_STRDIM_SIZE 256
static struct { char nm[16]; int d1; int d2;
 char data[BPP_MAX_STRDIM_SIZE][256]; } bpp_strdims[BPP_MAX_STRDIM];
static int bpp_strdim_count = 0;

static char *bpp_strdim_ref(const char *nm, int i1, int i2) {
 int k;
 (void)i2;
 for (k = 0; k < bpp_strdim_count; k++) {
 if (strcmp(bpp_strdims[k].nm, nm) == 0) {
 if (i1 < 0 || i1 >= BPP_MAX_STRDIM_SIZE) i1 = 0;
 return bpp_strdims[k].data[i1];
 }
 }
 /* Auto-create */
 if (bpp_strdim_count < BPP_MAX_STRDIM) {
 int j;
 strncpy(bpp_strdims[bpp_strdim_count].nm, nm, 15);
 bpp_strdims[bpp_strdim_count].nm[15] = '\0';
 for (j = 0; j < BPP_MAX_STRDIM_SIZE; j++)
 bpp_strdims[bpp_strdim_count].data[j][0] = '\0';
 bpp_strdim_count++;
 if (i1 < 0 || i1 >= BPP_MAX_STRDIM_SIZE) i1 = 0;
 return bpp_strdims[bpp_strdim_count-1].data[i1];
 }
 fprintf(stderr, "String array not found: %s\n", nm);
 exit(1);
 return NULL;
}

/* === DATA/READ support === */
#define BPP_MAX_DATA 4096
static const char *bpp_data_pool[BPP_MAX_DATA];
static int bpp_data_count = 0;
static int bpp_data_ptr = 0;

static double bpp_read_num(void) {
  if (bpp_data_ptr >= bpp_data_count) {
    fprintf(stderr, "OUT OF DATA\n"); exit(1);
  }
  return atof(bpp_data_pool[bpp_data_ptr++]);
}

static const char *bpp_read_str(void) {
  if (bpp_data_ptr >= bpp_data_count) {
    fprintf(stderr, "OUT OF DATA\n"); exit(1);
  }
  return bpp_data_pool[bpp_data_ptr++];
}

/* === String helpers === */
static char bpp_strtmp[8][256];
static int bpp_strtmp_idx = 0;

static char *bpp_tmp(void) {
 char *p = bpp_strtmp[bpp_strtmp_idx];
 bpp_strtmp_idx = (bpp_strtmp_idx+1) & 7;
 return p;
}

static const char *bpp_concat(const char *a, const char *b) {
 char *t = bpp_tmp();
 t[0]='\0';
 strncat(t, a, 254);
 strncat(t, b, 254-(int)strlen(t));
 return t;
}

static const char *bpp_left(const char *s, int n) {
 char *t = bpp_tmp();
 int len = (int)strlen(s);
 if (n>len) n=len;
 if (n<0) n=0;
 memcpy(t, s, (size_t)n); t[n]='\0';
 return t;
}

static const char *bpp_right(const char *s, int n) {
 char *t = bpp_tmp();
 int len = (int)strlen(s);
 if (n>len) n=len;
 if (n<0) n=0;
 memcpy(t, s+len-n, (size_t)n); t[n]='\0';
 return t;
}

static const char *bpp_mid(const char *s, int st, int n) {
 char *t = bpp_tmp();
 int len = (int)strlen(s);
 if (st<0) st=0;
 if (st>len) st=len;
 if (n<0) n=0;
 if (st+n>len) n=len-st;
 memcpy(t, s+st, (size_t)n); t[n]='\0';
 return t;
}

static const char *bpp_chr(int n) {
 char *t = bpp_tmp();
 t[0]=(char)n; t[1]='\0';
 return t;
}

static const char *bpp_str(double n) {
 char *t = bpp_tmp();
 sprintf(t, "%g", n);
 return t;
}

/* === TAB helper === */
static const char *bpp_tab(int col) {
 static char buf[256];
 int i;
 if (col < 0) col = 0;
 if (col > 255) col = 255;
 for (i = 0; i < col; i++) buf[i] = ' ';
 buf[col] = '\0';
 return buf;
}

/* === DEF FN support === */
#define BPP_MAX_FN 26
typedef double (*BppFnPtr)(double);
static double bpp_fn_param;

int main(void) {
  bpp_data_pool[0] = "0";
  bpp_data_pool[1] = "31";
  bpp_data_pool[2] = "28";
  bpp_data_pool[3] = "31";
  bpp_data_pool[4] = "30";
  bpp_data_pool[5] = "31";
  bpp_data_pool[6] = "30";
  bpp_data_pool[7] = "31";
  bpp_data_pool[8] = "31";
  bpp_data_pool[9] = "30";
  bpp_data_pool[10] = "31";
  bpp_data_pool[11] = "30";
  bpp_data_pool[12] = "31";
  bpp_data_count = 13;

 /* Line 10 */
 printf("%s", bpp_tab((int)(32)));
 printf("%s", "CALENDAR");
 printf("\n");
 /* Line 20 */
 printf("%s", bpp_tab((int)(15)));
 printf("%s", "CREATIVE COMPUTING  MORRISTOWN, NEW JERSEY");
 printf("\n");
 /* Line 30 */
  printf("\n");
  printf("\n");
  printf("\n");
 /* Line 100 */
 /* REM  - SEE NOTES */
 /* Line 110 */
 /* BASIC: DIM (Array Allocation) */
 bpp_dim_alloc("M", (int)(12), 0);
 /* Line 120 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_0:;
 printf("%s", bpp_chr((int)(10)));
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 6) goto bpp_for_0;
 /* Line 130 */
 /* REM  MONDAY (0=SUN, -1=MON, -2=TUES...) */
 bpp_vars[3] = 1;
  /* Line 140 */
 bpp_vars[18] = 0;
 /* Line 150 */
 /* REM  EACH MONTH */
 /* Line 160 */
 /* BASIC: FOR loop initialization */
 bpp_vars[13] = 0;
 bpp_for_1:;
 /* BASIC: READ statement */
 *bpp_dim_ref("M", (int)(bpp_vars[13]), 0) = bpp_read_num();
 /* BASIC: NEXT loop step */
 bpp_vars[13] += 1;
 if (bpp_vars[13] <= 12) goto bpp_for_1;
 /* Line 170 */
 /* REM  */
 /* Line 180 */
 /* BASIC: FOR loop initialization */
 bpp_vars[13] = 1;
 bpp_for_2:;
 /* Line 190 */
  printf("\n");
  printf("\n");
 bpp_vars[18] = (bpp_vars[18] + *bpp_dim_ref("M", (int)((bpp_vars[13] - 1)), 0));
 /* Line 200 */
 printf("%s", "**");
 { double _v = bpp_vars[18]; if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 printf("%s", bpp_tab((int)(7)));
 /* Line 210 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_3:;
 printf("%s", "*");
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 18) goto bpp_for_3;
 /* Line 220 */
 /* BASIC: ON GOTO branch */
 { int _sel = (int)(bpp_vars[13]);
  if (_sel == 1) goto L230;
  if (_sel == 2) goto L240;
  if (_sel == 3) goto L250;
  if (_sel == 4) goto L260;
  if (_sel == 5) goto L270;
  if (_sel == 6) goto L280;
  if (_sel == 7) goto L290;
  if (_sel == 8) goto L300;
  if (_sel == 9) goto L310;
  if (_sel == 10) goto L320;
  if (_sel == 11) goto L330;
  if (_sel == 12) goto L340;
 }
 L230: /* Line 230 */
 printf("%s", " JANUARY ");
 /* BASIC: GOTO */
 goto L350;
 L240: /* Line 240 */
 printf("%s", " FEBRUARY");
 /* BASIC: GOTO */
 goto L350;
 L250: /* Line 250 */
 printf("%s", "  MARCH  ");
 /* BASIC: GOTO */
 goto L350;
 L260: /* Line 260 */
 printf("%s", "  APRIL  ");
 /* BASIC: GOTO */
 goto L350;
 L270: /* Line 270 */
 printf("%s", "   MAY   ");
 /* BASIC: GOTO */
 goto L350;
 L280: /* Line 280 */
 printf("%s", "   JUNE  ");
 /* BASIC: GOTO */
 goto L350;
 L290: /* Line 290 */
 printf("%s", "   JULY  ");
 /* BASIC: GOTO */
 goto L350;
 L300: /* Line 300 */
 printf("%s", "  AUGUST ");
 /* BASIC: GOTO */
 goto L350;
 L310: /* Line 310 */
 printf("%s", "SEPTEMBER");
 /* BASIC: GOTO */
 goto L350;
 L320: /* Line 320 */
 printf("%s", " OCTOBER ");
 /* BASIC: GOTO */
 goto L350;
 L330: /* Line 330 */
 printf("%s", " NOVEMBER");
 /* BASIC: GOTO */
 goto L350;
 L340: /* Line 340 */
 printf("%s", " DECEMBER");
 L350: /* Line 350 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_4:;
 printf("%s", "*");
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 18) goto bpp_for_4;
 /* Line 360 */
 { double _v = (365 - bpp_vars[18]); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 printf("%s", "**");
 /* Line 370 */
 /* REM ;     ON LEAP YEARS */
 /* Line 380 */
 printf("%s", bpp_chr((int)(10)));
 printf("\n");
 printf("%s", "     S       M       T       W");
 /* Line 390 */
 printf("%s", "       T       F       S");
 printf("\n");
 /* Line 400 */
  printf("\n");
 /* Line 410 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_5:;
 printf("%s", "*");
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 59) goto bpp_for_5;
 /* Line 420 */
 /* REM  */
 /* Line 430 */
 /* BASIC: FOR loop initialization */
 bpp_vars[22] = 1;
 bpp_for_6:;
 /* Line 440 */
 printf("%s", bpp_chr((int)(10)));
 printf("\n");
 /* Line 450 */
 printf("%s", bpp_tab((int)(4)));
 printf("\n");
 /* Line 460 */
 /* REM  */
 /* Line 470 */
 /* BASIC: FOR loop initialization */
 bpp_vars[6] = 1;
 bpp_for_7:;
 /* Line 480 */
 bpp_vars[3] = (bpp_vars[3] + 1);
 /* Line 490 */
 bpp_vars[26] = (bpp_vars[3] - bpp_vars[18]);
 /* Line 500 */
 /* BASIC: IF statement */
 if ((bpp_vars[26] > *bpp_dim_ref("M", (int)(bpp_vars[13]), 0))) { /* BASIC: GOTO */
goto L580;
 }
 /* Line 510 */
 /* BASIC: IF statement */
 if ((bpp_vars[26] > 0)) { { double _v = bpp_vars[26]; if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 }
 /* Line 520 */
 printf("%s", bpp_tab((int)((4 + (8 * bpp_vars[6])))));
 /* Line 530 */
 /* BASIC: NEXT loop step */
 bpp_vars[6] += 1;
 if (bpp_vars[6] <= 7) goto bpp_for_7;
 /* Line 540 */
 /* REM  */
 /* Line 550 */
 /* BASIC: IF statement */
 if ((bpp_vars[26] == *bpp_dim_ref("M", (int)(bpp_vars[13]), 0))) { /* BASIC: GOTO */
goto L590;
 }
 /* Line 560 */
 /* BASIC: NEXT loop step */
 bpp_vars[22] += 1;
 if (bpp_vars[22] <= 6) goto bpp_for_6;
 /* Line 570 */
 /* REM  */
 L580: /* Line 580 */
 bpp_vars[3] = (bpp_vars[3] - bpp_vars[6]);
 L590: /* Line 590 */
 /* BASIC: NEXT loop step */
 bpp_vars[13] += 1;
 if (bpp_vars[13] <= 12) goto bpp_for_2;
 /* Line 600 */
 /* REM  */
 /* Line 610 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_8:;
 printf("%s", bpp_chr((int)(10)));
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 6) goto bpp_for_8;
 /* Line 620 */
 /* BASIC: DATA definition */
 /* DATA - see initialization */
 /* Line 630 */
 /* REM ,29,  ..., ON LEAP YEARS */
 /* Line 640 */
 goto bpp_end;
 bpp_end:
 return 0;
}
