#ifdef _MSC_VER
#ifndef _CRT_SECURE_NO_WARNINGS
#define _CRT_SECURE_NO_WARNINGS
#endif
#pragma warning(disable: 4996)
#pragma warning(disable: 4101)
#pragma warning(disable: 4244)
#pragma warning(disable: 4102)
#pragma warning(disable: 4311)
#pragma warning(disable: 4312)
#endif

#ifdef __GNUC__
#pragma GCC diagnostic ignored "-Wunused-function"
#pragma GCC diagnostic ignored "-Wunused-variable"
#pragma GCC diagnostic ignored "-Wunused-label"
#pragma GCC diagnostic ignored "-Wstringop-truncation"
#pragma GCC diagnostic ignored "-Wrestrict"
#pragma GCC diagnostic ignored "-Wdouble-promotion"
#pragma GCC diagnostic ignored "-Wpointer-to-int-cast"
#pragma GCC diagnostic ignored "-Wint-to-pointer-cast"
#endif

/* Generated by BASIC++ Compiler for target: windows */
#define BPP_TARGET_WINDOWS
#define BPP_SUPPORT_MULTITASKING 1
#define BPP_SUPPORT_GRAPHICS 1
#define BPP_SUPPORT_FILESYSTEM 1

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

/* Windows specific platform headers */
#include <windows.h>

/* === Runtime Variables === */
static double bpp_vars[1024]; /* A-Z and named */
static char bpp_strvars[1024][256]; /* A$-Z$ and named */
static double bpp_at_array[4096]; /* @() legacy array */
static int bpp_print_width = 14;

/* === GOSUB Stack === */
#define BPP_MAX_STACK 256
static int bpp_gosub_stack[BPP_MAX_STACK];
static int bpp_gosub_sp = 0;

/* === DIM Arrays === */
#define BPP_MAX_DIM 64
#define BPP_MAX_ELEM 4194304
static double bpp_dim_pool[BPP_MAX_ELEM];
static int bpp_dim_used = 0;
typedef struct { char name[32]; int d1,d2,off; } BppDim;
static BppDim bpp_dims[BPP_MAX_DIM];
static int bpp_dim_count = 0;

static void bpp_dim_alloc(const char *nm, int d1, int d2) {
 int sz = (d1+1) * (d2>0 ? d2+1 : 1);
 BppDim *d = &bpp_dims[bpp_dim_count++];
 strncpy(d->name, nm, 31); d->name[31]='\0';
 d->d1=d1; d->d2=d2; d->off=bpp_dim_used;
 memset(&bpp_dim_pool[bpp_dim_used], 0, sizeof(double)*(size_t)sz);
 bpp_dim_used += sz;
}

static double *bpp_dim_ref(const char *nm, int i1, int i2) {
 int k;
 for (k=0; k<bpp_dim_count; k++) {
 if (strcmp(bpp_dims[k].name, nm)==0) {
 int idx = (bpp_dims[k].d2>0)
 ? i1*(bpp_dims[k].d2+1)+i2 : i1;
 return &bpp_dim_pool[bpp_dims[k].off + idx];
 }
 }
 fprintf(stderr, "Array not found: %s\n", nm);
 exit(1);
 return NULL;
}

/* === String DIM arrays === */
#define BPP_MAX_STRDIM 26
#define BPP_MAX_STRDIM_SIZE 256
static struct { char nm[16]; int d1; int d2;
 char data[BPP_MAX_STRDIM_SIZE][256]; } bpp_strdims[BPP_MAX_STRDIM];
static int bpp_strdim_count = 0;

static char *bpp_strdim_ref(const char *nm, int i1, int i2) {
 int k;
 (void)i2;
 for (k = 0; k < bpp_strdim_count; k++) {
 if (strcmp(bpp_strdims[k].nm, nm) == 0) {
 if (i1 < 0 || i1 >= BPP_MAX_STRDIM_SIZE) i1 = 0;
 return bpp_strdims[k].data[i1];
 }
 }
 /* Auto-create */
 if (bpp_strdim_count < BPP_MAX_STRDIM) {
 int j;
 strncpy(bpp_strdims[bpp_strdim_count].nm, nm, 15);
 bpp_strdims[bpp_strdim_count].nm[15] = '\0';
 for (j = 0; j < BPP_MAX_STRDIM_SIZE; j++)
 bpp_strdims[bpp_strdim_count].data[j][0] = '\0';
 bpp_strdim_count++;
 if (i1 < 0 || i1 >= BPP_MAX_STRDIM_SIZE) i1 = 0;
 return bpp_strdims[bpp_strdim_count-1].data[i1];
 }
 fprintf(stderr, "String array not found: %s\n", nm);
 exit(1);
 return NULL;
}

/* === DATA/READ support === */
#define BPP_MAX_DATA 4096
static const char *bpp_data_pool[BPP_MAX_DATA];
static int bpp_data_count = 0;
static int bpp_data_ptr = 0;

static double bpp_read_num(void) {
  if (bpp_data_ptr >= bpp_data_count) {
    fprintf(stderr, "OUT OF DATA\n"); exit(1);
  }
  return atof(bpp_data_pool[bpp_data_ptr++]);
}

static const char *bpp_read_str(void) {
  if (bpp_data_ptr >= bpp_data_count) {
    fprintf(stderr, "OUT OF DATA\n"); exit(1);
  }
  return bpp_data_pool[bpp_data_ptr++];
}

/* === String helpers === */
static char bpp_strtmp[8][256];
static int bpp_strtmp_idx = 0;

static char *bpp_tmp(void) {
 char *p = bpp_strtmp[bpp_strtmp_idx];
 bpp_strtmp_idx = (bpp_strtmp_idx+1) & 7;
 return p;
}

static const char *bpp_concat(const char *a, const char *b) {
 char *t = bpp_tmp();
 t[0]='\0';
 strncat(t, a, 254);
 strncat(t, b, 254-(int)strlen(t));
 return t;
}

static const char *bpp_left(const char *s, int n) {
 char *t = bpp_tmp();
 int len = (int)strlen(s);
 if (n>len) n=len;
 if (n<0) n=0;
 memcpy(t, s, (size_t)n); t[n]='\0';
 return t;
}

static const char *bpp_right(const char *s, int n) {
 char *t = bpp_tmp();
 int len = (int)strlen(s);
 if (n>len) n=len;
 if (n<0) n=0;
 memcpy(t, s+len-n, (size_t)n); t[n]='\0';
 return t;
}

static const char *bpp_mid(const char *s, int st, int n) {
 char *t = bpp_tmp();
 int len = (int)strlen(s);
 if (st<0) st=0;
 if (st>len) st=len;
 if (n<0) n=0;
 if (st+n>len) n=len-st;
 memcpy(t, s+st, (size_t)n); t[n]='\0';
 return t;
}

static const char *bpp_chr(int n) {
 char *t = bpp_tmp();
 t[0]=(char)n; t[1]='\0';
 return t;
}

static const char *bpp_str(double n) {
 char *t = bpp_tmp();
 sprintf(t, "%g", n);
 return t;
}

/* === TAB helper === */
static const char *bpp_tab(int col) {
 static char buf[256];
 int i;
 if (col < 0) col = 0;
 if (col > 255) col = 255;
 for (i = 0; i < col; i++) buf[i] = ' ';
 buf[col] = '\0';
 return buf;
}

/* === DEF FN support === */
#define BPP_MAX_FN 26
typedef double (*BppFnPtr)(double);
static double bpp_fn_param;

int main(void) {
  bpp_data_count = 0;

 /* Line 5 */
 printf("%s", bpp_tab((int)(33)));
 printf("%s", "BATTLE");
 printf("\n");
 /* Line 7 */
 printf("%s", bpp_tab((int)(15)));
 printf("%s", "CREATIVE COMPUTING  MORRISTOWN, NEW JERSEY");
 printf("\n");
 /* Line 10 */
 /* REM  WRITTEN BY RAY WESTERGARD  10/70 */
 /* Line 20 */
 /* REM  THE REGENTS OF THE UNIV. OF CALIF. */
 /* Line 30 */
 /* REM  LAWRENCE HALL OF SCIENCE, BERKELEY */
 /* Line 40 */
 /* BASIC: DIM (Array Allocation) */
 bpp_dim_alloc("F", (int)(6), (int)(6));
 /* BASIC: DIM (Array Allocation) */
 bpp_dim_alloc("H", (int)(6), (int)(6));
 /* BASIC: DIM (Array Allocation) */
 bpp_dim_alloc("A", (int)(4), 0);
 /* BASIC: DIM (Array Allocation) */
 bpp_dim_alloc("B", (int)(4), 0);
 /* BASIC: DIM (Array Allocation) */
 bpp_dim_alloc("C", (int)(6), 0);
 /* BASIC: DIM (Array Allocation) */
 bpp_dim_alloc("L", (int)(3), 0);
 L50: /* Line 50 */
 /* BASIC: FOR loop initialization */
 bpp_vars[23] = 1;
 bpp_for_0:;
 /* Line 51 */
 /* BASIC: FOR loop initialization */
 bpp_vars[24] = 1;
 bpp_for_1:;
 /* Line 52 */
 *bpp_dim_ref("F", (int)(bpp_vars[23]), (int)(bpp_vars[24])) = 0;
 /* Line 53 */
 /* BASIC: NEXT loop step */
 bpp_vars[24] += 1;
 if (bpp_vars[24] <= 6) goto bpp_for_1;
 /* Line 54 */
 /* BASIC: NEXT loop step */
 bpp_vars[23] += 1;
 if (bpp_vars[23] <= 6) goto bpp_for_0;
 /* Line 60 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_2:;
 /* Line 70 */
 bpp_vars[13] = (4 - bpp_vars[8]);
 /* Line 80 */
 /* BASIC: FOR loop initialization */
 bpp_vars[9] = 1;
 bpp_for_3:;
 L90: /* Line 90 */
 bpp_vars[0] = floor(((6 * ((double)(rand() % (int)(1)))) + 1));
 /* Line 100 */
 bpp_vars[1] = floor(((6 * ((double)(rand() % (int)(1)))) + 1));
 /* Line 110 */
 bpp_vars[3] = floor(((4 * ((double)(rand() % (int)(1)))) + 1));
 /* Line 120 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)(bpp_vars[0]), (int)(bpp_vars[1])) > 0)) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 130 */
 bpp_vars[12] = 0;
 /* Line 140 */
 /* BASIC: ON GOTO branch */
 { int _sel = (int)(bpp_vars[3]);
  if (_sel == 1) goto L150;
  if (_sel == 2) goto L340;
  if (_sel == 3) goto L550;
  if (_sel == 4) goto L740;
 }
 L150: /* Line 150 */
 *bpp_dim_ref("B", (int)(1), 0) = bpp_vars[1];
 /* Line 160 */
 *bpp_dim_ref("B", (int)(2), 0) = 7;
 *bpp_dim_ref("B", (int)(3), 0) = 7;
 /* Line 170 */
 /* BASIC: FOR loop initialization */
 bpp_vars[10] = 1;
 bpp_for_4:;
 /* Line 180 */
 /* BASIC: IF statement */
 if ((bpp_vars[12] > 1)) { /* BASIC: GOTO */
goto L240;
 }
 /* Line 190 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) == 6)) { /* BASIC: GOTO */
goto L230;
 }
 /* Line 200 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)(bpp_vars[0]), (int)((*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) + 1))) > 0)) { /* BASIC: GOTO */
goto L230;
 }
 /* Line 210 */
 *bpp_dim_ref("B", (int)((bpp_vars[10] + 1)), 0) = (*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) + 1);
 /* Line 220 */
 /* BASIC: GOTO */
 goto L280;
 L230: /* Line 230 */
 bpp_vars[12] = 2;
 L240: /* Line 240 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(1), 0) < *bpp_dim_ref("B", (int)(2), 0))) && ((*bpp_dim_ref("B", (int)(1), 0) < *bpp_dim_ref("B", (int)(3), 0)))))) { bpp_vars[25] = *bpp_dim_ref("B", (int)(1), 0);
 }
 /* Line 242 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(2), 0) < *bpp_dim_ref("B", (int)(1), 0))) && ((*bpp_dim_ref("B", (int)(2), 0) < *bpp_dim_ref("B", (int)(3), 0)))))) { bpp_vars[25] = *bpp_dim_ref("B", (int)(2), 0);
 }
 /* Line 244 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(3), 0) < *bpp_dim_ref("B", (int)(1), 0))) && ((*bpp_dim_ref("B", (int)(3), 0) < *bpp_dim_ref("B", (int)(2), 0)))))) { bpp_vars[25] = *bpp_dim_ref("B", (int)(3), 0);
 }
 /* Line 250 */
 /* BASIC: IF statement */
 if ((bpp_vars[25] == 1)) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 260 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)(bpp_vars[0]), (int)((bpp_vars[25] - 1))) > 0)) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 270 */
 *bpp_dim_ref("B", (int)((bpp_vars[10] + 1)), 0) = (bpp_vars[25] - 1);
 L280: /* Line 280 */
 /* BASIC: NEXT loop step */
 bpp_vars[10] += 1;
 if (bpp_vars[10] <= bpp_vars[13]) goto bpp_for_4;
 /* Line 290 */
 *bpp_dim_ref("F", (int)(bpp_vars[0]), (int)(bpp_vars[1])) = ((9 - (2 * bpp_vars[8])) - bpp_vars[9]);
 /* Line 300 */
 /* BASIC: FOR loop initialization */
 bpp_vars[10] = 1;
 bpp_for_5:;
 /* Line 310 */
 *bpp_dim_ref("F", (int)(bpp_vars[0]), (int)(*bpp_dim_ref("B", (int)((bpp_vars[10] + 1)), 0))) = *bpp_dim_ref("F", (int)(bpp_vars[0]), (int)(bpp_vars[1]));
 /* Line 320 */
 /* BASIC: NEXT loop step */
 bpp_vars[10] += 1;
 if (bpp_vars[10] <= bpp_vars[13]) goto bpp_for_5;
 /* Line 330 */
 /* BASIC: GOTO */
 goto L990;
 L340: /* Line 340 */
 *bpp_dim_ref("A", (int)(1), 0) = bpp_vars[0];
 /* Line 350 */
 *bpp_dim_ref("B", (int)(1), 0) = bpp_vars[1];
 /* Line 360 */
 *bpp_dim_ref("A", (int)(2), 0) = 0;
 *bpp_dim_ref("A", (int)(3), 0) = 0;
 *bpp_dim_ref("B", (int)(2), 0) = 0;
 *bpp_dim_ref("B", (int)(3), 0) = 0;
 /* Line 370 */
 /* BASIC: FOR loop initialization */
 bpp_vars[10] = 1;
 bpp_for_6:;
 /* Line 380 */
 /* BASIC: IF statement */
 if ((bpp_vars[12] > 1)) { /* BASIC: GOTO */
goto L460;
 }
 /* Line 390 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) == 1)) || ((*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) == 1))))) { /* BASIC: GOTO */
goto L450;
 }
 /* Line 400 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) - 1)), (int)((*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) - 1))) > 0)) { /* BASIC: GOTO */
goto L450;
 }
 /* Line 410 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("F", (int)((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) - 1)), (int)(*bpp_dim_ref("B", (int)(bpp_vars[10]), 0))) > 0)) && ((*bpp_dim_ref("F", (int)((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) - 1)), (int)(*bpp_dim_ref("B", (int)(bpp_vars[10]), 0))) == *bpp_dim_ref("F", (int)(*bpp_dim_ref("A", (int)(bpp_vars[10]), 0)), (int)((*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) - 1)))))))) { /* BASIC: GOTO */
goto L450;
 }
 /* Line 420 */
 *bpp_dim_ref("A", (int)((bpp_vars[10] + 1)), 0) = (*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) - 1);
 /* Line 430 */
 *bpp_dim_ref("B", (int)((bpp_vars[10] + 1)), 0) = (*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) - 1);
 /* Line 440 */
 /* BASIC: GOTO */
 goto L530;
 L450: /* Line 450 */
 bpp_vars[12] = 2;
 L460: /* Line 460 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(1), 0) > *bpp_dim_ref("A", (int)(2), 0))) && ((*bpp_dim_ref("A", (int)(1), 0) > *bpp_dim_ref("A", (int)(3), 0)))))) { bpp_vars[26] = *bpp_dim_ref("A", (int)(1), 0);
 }
 /* Line 462 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(2), 0) > *bpp_dim_ref("A", (int)(1), 0))) && ((*bpp_dim_ref("A", (int)(2), 0) > *bpp_dim_ref("A", (int)(3), 0)))))) { bpp_vars[26] = *bpp_dim_ref("A", (int)(2), 0);
 }
 /* Line 464 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(3), 0) > *bpp_dim_ref("A", (int)(1), 0))) && ((*bpp_dim_ref("A", (int)(3), 0) > *bpp_dim_ref("A", (int)(2), 0)))))) { bpp_vars[26] = *bpp_dim_ref("A", (int)(3), 0);
 }
 /* Line 470 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(1), 0) > *bpp_dim_ref("B", (int)(2), 0))) && ((*bpp_dim_ref("B", (int)(1), 0) > *bpp_dim_ref("B", (int)(3), 0)))))) { bpp_vars[27] = *bpp_dim_ref("B", (int)(1), 0);
 }
 /* Line 474 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(2), 0) > *bpp_dim_ref("B", (int)(1), 0))) && ((*bpp_dim_ref("B", (int)(2), 0) > *bpp_dim_ref("B", (int)(3), 0)))))) { bpp_vars[27] = *bpp_dim_ref("B", (int)(2), 0);
 }
 /* Line 476 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(3), 0) > *bpp_dim_ref("B", (int)(1), 0))) && ((*bpp_dim_ref("B", (int)(3), 0) > *bpp_dim_ref("B", (int)(2), 0)))))) { bpp_vars[27] = *bpp_dim_ref("B", (int)(3), 0);
 }
 /* Line 480 */
 /* BASIC: IF statement */
 if (((((bpp_vars[26] == 6)) || ((bpp_vars[27] == 6))))) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 490 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)((bpp_vars[26] + 1)), (int)((bpp_vars[27] + 1))) > 0)) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 500 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("F", (int)(bpp_vars[26]), (int)((bpp_vars[27] + 1))) > 0)) && ((*bpp_dim_ref("F", (int)(bpp_vars[26]), (int)((bpp_vars[27] + 1))) == *bpp_dim_ref("F", (int)((bpp_vars[26] + 1)), (int)(bpp_vars[27]))))))) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 510 */
 *bpp_dim_ref("A", (int)((bpp_vars[10] + 1)), 0) = (bpp_vars[26] + 1);
 /* Line 520 */
 *bpp_dim_ref("B", (int)((bpp_vars[10] + 1)), 0) = (bpp_vars[27] + 1);
 L530: /* Line 530 */
 /* BASIC: NEXT loop step */
 bpp_vars[10] += 1;
 if (bpp_vars[10] <= bpp_vars[13]) goto bpp_for_6;
 /* Line 540 */
 /* BASIC: GOTO */
 goto L950;
 L550: /* Line 550 */
 *bpp_dim_ref("A", (int)(1), 0) = bpp_vars[0];
 /* Line 560 */
 *bpp_dim_ref("A", (int)(2), 0) = 7;
 *bpp_dim_ref("A", (int)(3), 0) = 7;
 /* Line 570 */
 /* BASIC: FOR loop initialization */
 bpp_vars[10] = 1;
 bpp_for_7:;
 /* Line 580 */
 /* BASIC: IF statement */
 if ((bpp_vars[12] > 1)) { /* BASIC: GOTO */
goto L640;
 }
 /* Line 590 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) == 6)) { /* BASIC: GOTO */
goto L630;
 }
 /* Line 600 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) + 1)), (int)(bpp_vars[1])) > 0)) { /* BASIC: GOTO */
goto L630;
 }
 /* Line 610 */
 *bpp_dim_ref("A", (int)((bpp_vars[10] + 1)), 0) = (*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) + 1);
 /* Line 620 */
 /* BASIC: GOTO */
 goto L680;
 L630: /* Line 630 */
 bpp_vars[12] = 2;
 L640: /* Line 640 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(1), 0) < *bpp_dim_ref("A", (int)(2), 0))) && ((*bpp_dim_ref("A", (int)(1), 0) < *bpp_dim_ref("A", (int)(3), 0)))))) { bpp_vars[25] = *bpp_dim_ref("A", (int)(1), 0);
 }
 /* Line 642 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(2), 0) < *bpp_dim_ref("A", (int)(1), 0))) && ((*bpp_dim_ref("A", (int)(2), 0) < *bpp_dim_ref("A", (int)(3), 0)))))) { bpp_vars[25] = *bpp_dim_ref("A", (int)(2), 0);
 }
 /* Line 644 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(3), 0) < *bpp_dim_ref("A", (int)(1), 0))) && ((*bpp_dim_ref("A", (int)(3), 0) < *bpp_dim_ref("A", (int)(2), 0)))))) { bpp_vars[25] = *bpp_dim_ref("A", (int)(3), 0);
 }
 /* Line 650 */
 /* BASIC: IF statement */
 if ((bpp_vars[25] == 1)) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 660 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)((bpp_vars[25] - 1)), (int)(bpp_vars[1])) > 0)) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 670 */
 *bpp_dim_ref("A", (int)((bpp_vars[10] + 1)), 0) = (bpp_vars[25] - 1);
 L680: /* Line 680 */
 /* BASIC: NEXT loop step */
 bpp_vars[10] += 1;
 if (bpp_vars[10] <= bpp_vars[13]) goto bpp_for_7;
 /* Line 690 */
 *bpp_dim_ref("F", (int)(bpp_vars[0]), (int)(bpp_vars[1])) = ((9 - (2 * bpp_vars[8])) - bpp_vars[9]);
 /* Line 700 */
 /* BASIC: FOR loop initialization */
 bpp_vars[10] = 1;
 bpp_for_8:;
 /* Line 710 */
 *bpp_dim_ref("F", (int)(*bpp_dim_ref("A", (int)((bpp_vars[10] + 1)), 0)), (int)(bpp_vars[1])) = *bpp_dim_ref("F", (int)(bpp_vars[0]), (int)(bpp_vars[1]));
 /* Line 720 */
 /* BASIC: NEXT loop step */
 bpp_vars[10] += 1;
 if (bpp_vars[10] <= bpp_vars[13]) goto bpp_for_8;
 /* Line 730 */
 /* BASIC: GOTO */
 goto L990;
 L740: /* Line 740 */
 *bpp_dim_ref("A", (int)(1), 0) = bpp_vars[0];
 /* Line 750 */
 *bpp_dim_ref("B", (int)(1), 0) = bpp_vars[1];
 /* Line 760 */
 *bpp_dim_ref("A", (int)(2), 0) = 7;
 *bpp_dim_ref("A", (int)(3), 0) = 7;
 /* Line 770 */
 *bpp_dim_ref("B", (int)(2), 0) = 0;
 *bpp_dim_ref("B", (int)(3), 0) = 0;
 /* Line 780 */
 /* BASIC: FOR loop initialization */
 bpp_vars[10] = 1;
 bpp_for_9:;
 /* Line 790 */
 /* BASIC: IF statement */
 if ((bpp_vars[12] > 1)) { /* BASIC: GOTO */
goto L870;
 }
 /* Line 800 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) == 6)) || ((*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) == 1))))) { /* BASIC: GOTO */
goto L860;
 }
 /* Line 810 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) + 1)), (int)((*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) - 1))) > 0)) { /* BASIC: GOTO */
goto L860;
 }
 /* Line 820 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("F", (int)((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) + 1)), (int)(*bpp_dim_ref("B", (int)(bpp_vars[10]), 0))) > 0)) && ((*bpp_dim_ref("F", (int)((*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) + 1)), (int)(*bpp_dim_ref("B", (int)(bpp_vars[10]), 0))) == *bpp_dim_ref("F", (int)(*bpp_dim_ref("A", (int)(bpp_vars[10]), 0)), (int)((*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) - 1)))))))) { /* BASIC: GOTO */
goto L860;
 }
 /* Line 830 */
 *bpp_dim_ref("A", (int)((bpp_vars[10] + 1)), 0) = (*bpp_dim_ref("A", (int)(bpp_vars[10]), 0) + 1);
 /* Line 840 */
 *bpp_dim_ref("B", (int)((bpp_vars[10] + 1)), 0) = (*bpp_dim_ref("B", (int)(bpp_vars[10]), 0) - 1);
 /* Line 850 */
 /* BASIC: GOTO */
 goto L940;
 L860: /* Line 860 */
 bpp_vars[12] = 2;
 L870: /* Line 870 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(1), 0) < *bpp_dim_ref("A", (int)(2), 0))) && ((*bpp_dim_ref("A", (int)(1), 0) < *bpp_dim_ref("A", (int)(3), 0)))))) { bpp_vars[26] = *bpp_dim_ref("A", (int)(1), 0);
 }
 /* Line 872 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(2), 0) < *bpp_dim_ref("A", (int)(1), 0))) && ((*bpp_dim_ref("A", (int)(2), 0) < *bpp_dim_ref("A", (int)(3), 0)))))) { bpp_vars[26] = *bpp_dim_ref("A", (int)(2), 0);
 }
 /* Line 874 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("A", (int)(3), 0) < *bpp_dim_ref("A", (int)(1), 0))) && ((*bpp_dim_ref("A", (int)(3), 0) < *bpp_dim_ref("A", (int)(2), 0)))))) { bpp_vars[26] = *bpp_dim_ref("A", (int)(3), 0);
 }
 /* Line 880 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(1), 0) > *bpp_dim_ref("B", (int)(2), 0))) && ((*bpp_dim_ref("B", (int)(1), 0) > *bpp_dim_ref("B", (int)(3), 0)))))) { bpp_vars[27] = *bpp_dim_ref("B", (int)(1), 0);
 }
 /* Line 882 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(2), 0) > *bpp_dim_ref("B", (int)(1), 0))) && ((*bpp_dim_ref("B", (int)(2), 0) > *bpp_dim_ref("B", (int)(3), 0)))))) { bpp_vars[27] = *bpp_dim_ref("B", (int)(2), 0);
 }
 /* Line 884 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("B", (int)(3), 0) > *bpp_dim_ref("B", (int)(1), 0))) && ((*bpp_dim_ref("B", (int)(3), 0) > *bpp_dim_ref("B", (int)(2), 0)))))) { bpp_vars[27] = *bpp_dim_ref("B", (int)(3), 0);
 }
 /* Line 890 */
 /* BASIC: IF statement */
 if (((((bpp_vars[26] == 1)) || ((bpp_vars[27] == 6))))) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 900 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)((bpp_vars[26] - 1)), (int)((bpp_vars[27] + 1))) > 0)) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 910 */
 /* BASIC: IF statement */
 if (((((*bpp_dim_ref("F", (int)(bpp_vars[26]), (int)((bpp_vars[27] + 1))) > 0)) && ((*bpp_dim_ref("F", (int)(bpp_vars[26]), (int)((bpp_vars[27] + 1))) == *bpp_dim_ref("F", (int)((bpp_vars[26] - 1)), (int)(bpp_vars[27]))))))) { /* BASIC: GOTO */
goto L90;
 }
 /* Line 920 */
 *bpp_dim_ref("A", (int)((bpp_vars[10] + 1)), 0) = (bpp_vars[26] - 1);
 /* Line 930 */
 *bpp_dim_ref("B", (int)((bpp_vars[10] + 1)), 0) = (bpp_vars[27] + 1);
 L940: /* Line 940 */
 /* BASIC: NEXT loop step */
 bpp_vars[10] += 1;
 if (bpp_vars[10] <= bpp_vars[13]) goto bpp_for_9;
 L950: /* Line 950 */
 *bpp_dim_ref("F", (int)(bpp_vars[0]), (int)(bpp_vars[1])) = ((9 - (2 * bpp_vars[8])) - bpp_vars[9]);
 /* Line 960 */
 /* BASIC: FOR loop initialization */
 bpp_vars[10] = 1;
 bpp_for_10:;
 /* Line 970 */
 *bpp_dim_ref("F", (int)(*bpp_dim_ref("A", (int)((bpp_vars[10] + 1)), 0)), (int)(*bpp_dim_ref("B", (int)((bpp_vars[10] + 1)), 0))) = *bpp_dim_ref("F", (int)(bpp_vars[0]), (int)(bpp_vars[1]));
 /* Line 980 */
 /* BASIC: NEXT loop step */
 bpp_vars[10] += 1;
 if (bpp_vars[10] <= bpp_vars[13]) goto bpp_for_10;
 L990: /* Line 990 */
 /* BASIC: NEXT loop step */
 bpp_vars[9] += 1;
 if (bpp_vars[9] <= 2) goto bpp_for_3;
 /* Line 1000 */
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 3) goto bpp_for_2;
 /* Line 1010 */
  printf("\n");
 /* Line 1020 */
 printf("%s", "THE FOLLOWING CODE OF THE BAD GUYS' FLEET DISPOSITION");
 printf("\n");
 /* Line 1030 */
 printf("%s", "HAS BEEN CAPTURED BUT NOT DECODED:");
 printf("\n");
 /* Line 1040 */
  printf("\n");
 /* Line 1050 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_11:;
 /* Line 1051 */
 /* BASIC: FOR loop initialization */
 bpp_vars[9] = 1;
 bpp_for_12:;
 /* Line 1052 */
 *bpp_dim_ref("H", (int)(bpp_vars[8]), (int)(bpp_vars[9])) = *bpp_dim_ref("F", (int)(bpp_vars[9]), (int)(bpp_vars[8]));
 /* Line 1053 */
 /* BASIC: NEXT loop step */
 bpp_vars[9] += 1;
 if (bpp_vars[9] <= 6) goto bpp_for_12;
 /* Line 1054 */
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 6) goto bpp_for_11;
 /* Line 1060 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_13:;
 /* Line 1061 */
 /* BASIC: FOR loop initialization */
 bpp_vars[9] = 1;
 bpp_for_14:;
 /* Line 1062 */
 { double _v = *bpp_dim_ref("H", (int)(bpp_vars[8]), (int)(bpp_vars[9])); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 /* Line 1063 */
 /* BASIC: NEXT loop step */
 bpp_vars[9] += 1;
 if (bpp_vars[9] <= 6) goto bpp_for_14;
 /* Line 1064 */
  printf("\n");
 /* Line 1065 */
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 6) goto bpp_for_13;
 /* Line 1070 */
  printf("\n");
 /* Line 1080 */
 printf("%s", "DE-CODE IT AND USE IT IF YOU CAN");
 printf("\n");
 /* Line 1090 */
 printf("%s", "BUT KEEP THE DE-CODING METHOD A SECRET.");
 printf("\n");
 /* Line 1100 */
  printf("\n");
 /* Line 1110 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_15:;
 /* Line 1111 */
 /* BASIC: FOR loop initialization */
 bpp_vars[9] = 1;
 bpp_for_16:;
 /* Line 1112 */
 *bpp_dim_ref("H", (int)(bpp_vars[8]), (int)(bpp_vars[9])) = 0;
 /* Line 1113 */
 /* BASIC: NEXT loop step */
 bpp_vars[9] += 1;
 if (bpp_vars[9] <= 6) goto bpp_for_16;
 /* Line 1114 */
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 6) goto bpp_for_15;
 /* Line 1120 */
 /* BASIC: FOR loop initialization */
 bpp_vars[8] = 1;
 bpp_for_17:;
 /* Line 1121 */
 *bpp_dim_ref("L", (int)(bpp_vars[8]), 0) = 0;
 /* Line 1122 */
 /* BASIC: NEXT loop step */
 bpp_vars[8] += 1;
 if (bpp_vars[8] <= 3) goto bpp_for_17;
 /* Line 1130 */
 *bpp_dim_ref("C", (int)(1), 0) = 2;
 *bpp_dim_ref("C", (int)(2), 0) = 2;
 /* Line 1140 */
 *bpp_dim_ref("C", (int)(3), 0) = 1;
 *bpp_dim_ref("C", (int)(4), 0) = 1;
 /* Line 1150 */
 *bpp_dim_ref("C", (int)(5), 0) = 0;
 *bpp_dim_ref("C", (int)(6), 0) = 0;
 /* Line 1160 */
 bpp_vars[18] = 0;
 bpp_vars[7] = 0;
 /* Line 1170 */
 printf("%s", "START GAME");
 printf("\n");
 L1180: /* Line 1180 */
 printf("? ");
 fflush(stdout);
 { char _buf[256]; double _v;
   fgets(_buf, 256, stdin); _v = atof(_buf);
   bpp_vars[23] = _v; }
 fflush(stdout);
 { char _buf[256]; double _v;
   fgets(_buf, 256, stdin); _v = atof(_buf);
   bpp_vars[24] = _v; }
 /* Line 1190 */
 /* BASIC: IF statement */
 if ((((((((bpp_vars[23] < 1)) || ((bpp_vars[23] > 6))))) || ((floor(bpp_vars[23]) != fabs(bpp_vars[23])))))) { /* BASIC: GOTO */
goto L1210;
 }
 /* Line 1200 */
 /* BASIC: IF statement */
 if ((((((((bpp_vars[24] > 0)) && ((bpp_vars[24] < 7))))) && ((floor(bpp_vars[24]) == fabs(bpp_vars[24])))))) { /* BASIC: GOTO */
goto L1230;
 }
 L1210: /* Line 1210 */
 printf("%s", "INVALID INPUT.  TRY AGAIN.");
 printf("\n");
 /* Line 1220 */
 /* BASIC: GOTO */
 goto L1180;
 L1230: /* Line 1230 */
 bpp_vars[17] = (7 - bpp_vars[24]);
 /* Line 1240 */
 bpp_vars[2] = bpp_vars[23];
 /* Line 1250 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2])) > 0)) { /* BASIC: GOTO */
goto L1290;
 }
 /* Line 1260 */
 bpp_vars[18] = (bpp_vars[18] + 1);
 /* Line 1270 */
 printf("%s", "SPLASH!  TRY AGAIN.");
 printf("\n");
 /* Line 1280 */
 /* BASIC: GOTO */
 goto L1180;
 L1290: /* Line 1290 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("C", (int)(*bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2]))), 0) < 4)) { /* BASIC: GOTO */
goto L1340;
 }
 /* Line 1300 */
 printf("%s", "THERE USED TO BE A SHIP AT THAT POINT, BUT YOU SUNK IT.");
 printf("\n");
 /* Line 1310 */
 printf("%s", "SPLASH!  TRY AGAIN.");
 printf("\n");
 /* Line 1320 */
 bpp_vars[18] = (bpp_vars[18] + 1);
 /* Line 1330 */
 /* BASIC: GOTO */
 goto L1180;
 L1340: /* Line 1340 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("H", (int)(bpp_vars[17]), (int)(bpp_vars[2])) > 0)) { /* BASIC: GOTO */
goto L1420;
 }
 /* Line 1350 */
 bpp_vars[7] = (bpp_vars[7] + 1);
 /* Line 1360 */
 *bpp_dim_ref("H", (int)(bpp_vars[17]), (int)(bpp_vars[2])) = *bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2]));
 /* Line 1370 */
 printf("%s", "A DIRECT HIT ON SHIP NUMBER");
 { double _v = *bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2])); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 printf("\n");
 /* Line 1380 */
 *bpp_dim_ref("C", (int)(*bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2]))), 0) = (*bpp_dim_ref("C", (int)(*bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2]))), 0) + 1);
 /* Line 1390 */
 /* BASIC: IF statement */
 if ((*bpp_dim_ref("C", (int)(*bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2]))), 0) >= 4)) { /* BASIC: GOTO */
goto L1470;
 }
 /* Line 1400 */
 printf("%s", "TRY AGAIN.");
 printf("\n");
 /* Line 1410 */
 /* BASIC: GOTO */
 goto L1180;
 L1420: /* Line 1420 */
 printf("%s", "YOU ALREADY PUT A HOLE IN SHIP NUMBER");
 { double _v = *bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2])); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 /* Line 1430 */
 printf("%s", "AT THAT POINT.");
 printf("\n");
 /* Line 1440 */
 printf("%s", "SPLASH!  TRY AGAIN.");
 printf("\n");
 /* Line 1450 */
 bpp_vars[18] = (bpp_vars[18] + 1);
 /* Line 1460 */
 /* BASIC: GOTO */
 goto L1180;
 L1470: /* Line 1470 */
 *bpp_dim_ref("L", (int)(((floor((*bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2])) - 1)) / 2) + 1)), 0) = (*bpp_dim_ref("L", (int)(((floor((*bpp_dim_ref("F", (int)(bpp_vars[17]), (int)(bpp_vars[2])) - 1)) / 2) + 1)), 0) + 1);
 /* Line 1480 */
 printf("%s", "AND YOU SUNK IT.  HURRAH FOR THE GOOD GUYS.");
 printf("\n");
 /* Line 1490 */
 printf("%s", "SO FAR, THE BAD GUYS HAVE LOST");
 printf("\n");
 /* Line 1500 */
 { double _v = *bpp_dim_ref("L", (int)(1), 0); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 printf("%s", "DESTROYER(S),");
 { double _v = *bpp_dim_ref("L", (int)(2), 0); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 printf("%s", "CRUISER(S), AND");
 /* Line 1510 */
 { double _v = *bpp_dim_ref("L", (int)(3), 0); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 printf("%s", "AIRCRAFT CARRIER(S).");
 printf("\n");
 /* Line 1520 */
 printf("%s", "YOUR CURRENT SPLASH/HIT RATIO IS");
 { double _v = (bpp_vars[18] / bpp_vars[7]); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 printf("\n");
 /* Line 1530 */
 /* BASIC: IF statement */
 if ((((*bpp_dim_ref("L", (int)(1), 0) + *bpp_dim_ref("L", (int)(2), 0)) + *bpp_dim_ref("L", (int)(3), 0)) < 6)) { /* BASIC: GOTO */
goto L1180;
 }
 /* Line 1540 */
  printf("\n");
 /* Line 1550 */
 printf("%s", "YOU HAVE TOTALLY WIPED OUT THE BAD GUYS' FLEET");
 printf("\n");
 /* Line 1560 */
 printf("%s", "WITH A FINAL SPLASH/HIT RATIO OF");
 { double _v = (bpp_vars[18] / bpp_vars[7]); if (_v == (double)(long)_v) printf("%*ld", bpp_print_width, (long)_v); else printf("%g", _v); }
 printf("\n");
 /* Line 1570 */
 /* BASIC: IF statement */
 if (((bpp_vars[18] / bpp_vars[7]) > 0)) { /* BASIC: GOTO */
goto L1590;
 }
 /* Line 1580 */
 printf("%s", "CONGRATULATIONS -- A DIRECT HIT EVERY TIME.");
 printf("\n");
 L1590: /* Line 1590 */
  printf("\n");
 /* Line 1600 */
 printf("%s", "****************************");
 printf("\n");
 /* Line 1610 */
  printf("\n");
 /* Line 1620 */
 /* BASIC: GOTO */
 goto L50;
 /* Line 1630 */
 goto bpp_end;
 bpp_end:
 return 0;
}
