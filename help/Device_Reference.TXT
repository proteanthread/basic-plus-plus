==============================================================================
Title:        Device_Reference
Tier:         2
Applies to:   BASIC++ v6.5.2 (baspp, bpp, bs, iot)
Authority:    engine/src/device/vdev.c, engine/src/device/bus.c
Generated:    no, hand-written
Status:       current
==============================================================================

BASIC++ VIRTUAL DEVICE SUBSYSTEM & VHAL MASTER REFERENCE

The authoritative reference catalog for the Virtual Hardware Abstraction
Layer (VHAL), universal sub-device indexing laws, registered virtual devices
across vintage ecosystems, programmatic device discovery, and device control
statements in BASIC++.


1. ARCHITECTURAL OVERVIEW & UNIVERSAL SUB-DEVICE LAW

The BASIC++ Virtual Device Subsystem (libkernel / engine/src/device/vdev.c)
provides a unified, cross-ecosystem hardware abstraction connecting BASIC
programs, C micro-libraries, and host hardware peripherals.

Sub-Device Normalization and Indexing Rules:

1. Unindexed Normalization: Any device referenced without an explicit
   sub-device number (e.g. LOOP:, CAS:, MDM:, CON:, CONS:, COM:)
   automatically normalizes to sub-device 1 (e.g. LOOP1:, CAS1:, MDM1:,
   CON1:, CONS1:, COM1:).
2. User Sub-Channels (DEV1: through DEV8:): Channel slots 1 through 8 are
   allocated for standard user applications, peripheral ports, and
   communication channels.
3. Supervisor Reservation Guard (DEV0: and DEV9:):
   - DEV0: is strictly reserved for system loopback diagnostics, supervisor
     channels, and the LOGGER syslog virtual sink.
   - DEV9: is strictly reserved for the master bus broadcast sink and WALL:
     messaging bus.
   - Accessing DEV0: or DEV9: via standard user OPEN operations triggers
     Error 5 (Illegal Function Call).
4. Zero Built-In Aliases Invariant: The engine contains zero pre-mapped
   built-in aliases. All device families are registered as authentic
   first-class drivers with distinct hardware personalities. Aliasing
   (DEVICE ALIAS ... AS ... or DEVICE BIND ... TO ...) is reserved strictly
   for user-defined mappings at runtime.


2. MASTER CATALOG OF REGISTERED VIRTUAL DEVICES

Prefix      Ecosystem / Std        Class               Description
----------  ---------------------  ------------------  --------------------
CON1..CON8  Master Dialect         VDCLASS_CONSOLE     Console (screen+kbd)
CONS1..8    Tymshare Super BASIC   VDCLASS_CONSOLE     Multi-user console
TTY1..TTY8  Unix / POSIX           VDCLASS_CONSOLE     Terminal ports
KEY1..KEY8  Hardware               VDCLASS_HID         Keyboard input only
SCN1..SCN8  Hardware               VDCLASS_DISPLAY     Screen output only
WORKSTN1..8 DEC / IBM / VAX        VDCLASS_CONSOLE     Workstation terminal
TEK1..TEK8  Tektronix 4010/4014    VDCLASS_CONSOLE     Vector storage tube
52501..52508 IBM System/3X         VDCLASS_CONSOLE     Twinax 5250/3270
LOOP1..8    Diagnostic / VHAL      VDCLASS_LOOPBACK    FIFO loopback buffer
USER1..8    Multi-User Sessions    VDCLASS_USER        IPC message queues
CAS1..CAS8  Vintage Audio / Tape   VDCLASS_TAPE        Cassette FSK audio
TAP1..TAP8  Tape Containers        VDCLASS_TAPE        Magnetic tape driver
C:          Atari CIO              VDCLASS_TAPE        Atari audio cassette
MAG1..MAG8  Magnetic Tape          VDCLASS_TAPE        General tape stream
MDM1..MDM8  Tandy / TRS-80         VDCLASS_MODEM       Hayes modem emulator
TEL1..TEL8  Tandy Model 100        VDCLASS_CUSTOM      Telephony interface
LCD1..LCD8  Tandy Portable         VDCLASS_DISPLAY     Liquid crystal disp
BAR1..BAR8  Tandy TRS-80           VDCLASS_HID         Barcode wand reader
WND1..WND8  Tandy TRS-80           VDCLASS_HID         Barcode scanner wand
LP1..LP8    Tandy / TRS-80         VDCLASS_PRINTER     Line Printer stream
FDD1..FDD8  Tandy Portable Disk    VDCLASS_STORAGE     Portable disk drive
KBD1..KBD8  Programmable HID       VDCLASS_KEYMAP      Keymap matrices
SCR1..SCR8  BGI / Raster           VDCLASS_FRAMEBUFFER Framebuffer canvases
LPT1..LPT8  MS-DOS / PC-DOS        VDCLASS_PRINTER     Parallel printer 1-8
PRN1..PRN8  MS-DOS / PC-DOS        VDCLASS_PRINTER     Standard printer out
LST1..LST8  CP/M / MBASIC          VDCLASS_PRINTER     Listing printer
PDF1..PDF8  Vector Graphics        VDCLASS_PRINTER     Direct PDF generator
COM1..COM8  RS-232 / UART          VDCLASS_SERIAL      Serial ports (UART)
AUX1..AUX8  MS-DOS / CP/M          VDCLASS_SERIAL      Auxiliary serial
E:          Atari CIO              VDCLASS_CONSOLE     Screen Editor window
K:          Atari CIO              VDCLASS_HID         Raw Keyboard stream
S:          Atari CIO              VDCLASS_DISPLAY     Screen draw surface
D1..D8      Atari CIO              VDCLASS_STORAGE     Atari disk drives
P:          Atari CIO              VDCLASS_PRINTER     Atari Line Printer
R1..R8      Atari CIO              VDCLASS_SERIAL      Atari 850 serial
V:          Atari CIO              VDCLASS_CUSTOM      Voice Synthesizer
PUN: / PTP: CP/M 2.2 / MP/M        VDCLASS_STORAGE     Paper Tape Punch
RDR: / PTR: CP/M 2.2 / MP/M        VDCLASS_STORAGE     Paper Tape Reader
AXI: / AXO: CP/M 2.2 / MP/M        VDCLASS_SERIAL      Auxiliary I/O streams


3. PROGRAMMATIC DEVICE INTROSPECTION FUNCTIONS

BASIC++ provides built-in functions allowing programs to discover and query
registered devices dynamically at runtime:

- DEVICES$: Returns the complete list of registered virtual devices as a
  newline-delimited string.
- DEVSTATUS$(slot): Returns the operational status of the device in slot
  number slot as an uppercase string: "ACTIVE", "IDLE", "DISABLED", or
  "EMPTY".
- DEVNAME$(slot): Returns the unique registered name of the device in slot
  number slot (e.g. "Console", "VirtFS", "Network").
- DEVTYPE$(slot): Returns the class or type identifier string of the device
  in slot number slot (e.g. "VCon", "File", "VNet", "BGI").

Dynamic Introspection Example:

    10 FOR S = 0 TO 15
    20   T$ = DEVTYPE$(S)
    30   IF T$ <> "" AND T$ <> "EMPTY" THEN
    40     PRINT "Slot "; S; ": "; DEVNAME$(S); " ["; T$; "]"
    45     PRINT "        Status="; DEVSTATUS$(S)
    50   END IF
    60 NEXT S

Feature Detection with Defensive Blocks:

    10 TRY
    20   SCREEN 12
    30   GraphicsReady = 1
    40 CATCH
    50   GraphicsReady = 0
    60   PRINT "Graphics unavailable, continuing in text mode."
    70 END TRY


4. DEVICE MANAGEMENT STATEMENTS

Virtual devices are managed interactively or programmatically via the DEVICE
and DEVICES statements:

Statement Syntax:

    DEVICES                                         ' List devices/aliases
    DEVICE "dev_name:", "config_param=val"          ' Configure parameters
    DEVICE ALIAS "alias:" AS "target:"              ' Create user alias
    DEVICE UNALIAS "alias:"                         ' Remove user alias
    DEVICE MOUNT "dev:" TYPE "driver" [OPTIONS "o"] ' Mount dynamic driver
    DEVICE RESET                                    ' Reset to defaults

Practical Device Command Examples:

    10 REM List all active devices
    20 DEVICES
    30 REM Create an alias mapping COM1 to standard serial port
    40 DEVICE ALIAS "MODEM:" AS "COM1:"
    50 OPEN "MODEM:9600,N,8,1" FOR RANDOM AS #1
    60 CLOSE #1
    70 DEVICE UNALIAS "MODEM:"
