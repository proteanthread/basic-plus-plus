10 IF LEFT$("HELLO", 2) <> "HE" THEN PRINT "LEFT$ FAILED" : END
20 PRINT "LEFT$ PASSED"
30 IF LEFT$("", 5) <> "" THEN PRINT "LEFT$ EMPTY FAILED": GOTO 9999
40 IF LEFT$("ABC", 0) <> "" THEN PRINT "LEFT$ ZERO FAILED": GOTO 9999
50 IF LEFT$("ABC", 500) <> "ABC" THEN PRINT "LEFT$ OVERFLOW FAILED": GOTO 9999
