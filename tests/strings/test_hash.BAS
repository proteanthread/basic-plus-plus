10 PRINT "--- HASH TEST ---"
20 OPTION STRICT OFF
30 H1 = HASH("TEST")
40 IF H1 = 0 THEN PRINT "HASH FAILED": GOTO 9999
50 H2$ = HASH$("TEST")
60 IF LEN(H2$) < 2 THEN PRINT "HASH$ FAILED": GOTO 9999
70 PRINT "ALL TESTS PASSED"
80 END
9999 PRINT "TEST SUITE FAILED"
